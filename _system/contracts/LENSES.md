# Discovery lenses — moved to the template

The lens roster now lives in the template, so a repo carries it with no icm-board in reach
(D45): **[`template/icm-pipeline/_shared/lenses.md`](../template/icm-pipeline/_shared/lenses.md)**
— `T`, synced to every pipeline repo as `.icm/_shared/lenses.md`. Edit the roster there;
`/project` and the `project-lens` agent both read it.

This file stays only so existing links resolve. One home per fact — never restate the roster
here.
