# icm-board — project register

> Seeded 2026-08-26 at the split from `k0d0minio/jamienisbet`, from intent Jamie stated
> directly in that session. **Not yet a `/project` run** — the empty sections below are
> real gaps, not omissions. Run `/project icm-board` to interrogate and fill them.
> Maintained by `/project`. Amend by re-running it, not by hand-editing during a session.

## What this is

The second brain of Jamie Nisbet's business, written as an ICM system: three workspaces
(`sell`, `start`, `deliver`) that carry the whole client lifecycle as stage contracts
with human gates; the knowledge layer they cite (services, pricing, voice, terms,
stack); the contracts every repo is measured against; the scripts that measure them; and
the canonical Claude assets seeded across the estate. It holds no application code and
ships no product. Its one user is Jamie; its jobs are that every deal moves through a
defined, improvable process, and every repo in the estate stays aligned.

## Intent

- **For whom** — Jamie, and every Claude session opened at `~/Apps` or in any estate repo.
- **The job** — two, and the first wins when they conflict: (1) the business's processes
  and knowledge live here, versioned and improvable, so no deal reinvents them;
  (2) the estate stays conformant, with drift visible before it compounds.
- **Done looks like** — a lead can be taken from first contact to a running project by
  walking folders anyone could read; a repo can be adopted, analysed and ticketed
  without anyone remembering how; and both say so when they drift.
- **Explicitly not** — a pipeline driver. The folders are the orchestration; stages run
  when Jamie enters them, and no outbound action leaves a session. The house rule
  "never build an orchestrator" survives the return of the business workspaces, and
  outranks everything here.

## Business logic

- **The repo wins.** Where a repo's own contracts conflict with the estate baseline, the
  repo is authoritative. Sustentus is the standing example and is exempt outright.
- **Tickets live next to the logic they describe.** A ticket about `_system/scripts/` lives
  here; a ticket about the dashboard lives in `jamienisbet`. This is why the `JN-*` series
  was split at the 2026-08-26 separation.
- **Gates are human checkboxes.** Read, never tick. Anything that cannot be verified from
  the repo (a Vercel setting, a token, a secret) is a human ticket that says so in its
  Prompt.
- **Conformance reports, it does not repair.** `icm-check.sh --fix` seeds only what is
  missing and never overwrites; the scheduled workflow reports and never writes.

## Features
| Feature | State | Tickets |
|---|---|---|
| Four commands — `/client`, `/project`, `/day`, `/icm-check` (thin routers) | shipped | — |
| Sell workspace — 01_intake → 02_look → 03_quote → 04_proposal → 05_agreement (five-part contracts, forms, agreement templates en/fr/pt) | rewritten 2026-09-22, unproven | — |
| Start workspace — 06_onboarding → 07_kickoff (07 absorbs the repo step; created at signature by default) | rewritten 2026-09-22, unproven | — |
| Deliver workspace — the three rituals as stage contracts | shipped | — |
| Knowledge layer — services, pricing, voice, terms, stack | shipped | — |
| Canonical Claude asset library + drift report | shipped | — |
| Four estate scripts — icm-check, tickets-board, ticket-hygiene, pull-all | shipped | — |
| Self-check CI — shellcheck, contract links, ticket lint | shipped | — |
| Remote conformance CI over the `k0d0minio` org | shipped | — |
| Estate pipeline — intake epics + the one pipeline weighted by `complexity` (D22; the profile tiers are gone), extracted from sustentus; the agency layer (D23) and the disjoint-cut rules (D26) added 2026-09-22; capability skills, advisor–executor routing, per-run databases + millisecond migrations, the security gate and the canonical run pack (D29) added 2026-09-23; estate rollout pending | shipped | — (rollout banked: _done/estate-migration/) |
| Estate heartbeat — scheduled digest | out | — dropped 2026-08-28 (fresh-footing sweep); AUDIT #8 keeps the want |
| `/day` run log — folded into the heartbeat | out | — dropped with the heartbeat |

## Constraints

- **The estate is half-invisible to CI.** Client repos are gitignored and live only on this
  machine, so anything running in Actions must reach them over the GitHub API and is
  limited to what a read-only token can see.
- **Legal / data** — client repo names and business state are not public; this repo is
  private and `_system/AUDIT.md` is one reason why.
- **Commercial** — solo operator. Every rule here has to earn its maintenance cost.

## Decisions
| ID | Decision | Date | Supersedes |
|---|---|---|---|
| D1 | The control layer gets its own repo, `icm-board`; `jamienisbet` becomes a normal repo under `projects/` and keeps its remote, CI and tickets | 2026-08-26 | the 2026-08-12 consolidation |
| D2 | Ticketing and workflows live next to the code whose logic they describe, not centrally | 2026-08-26 | — |
| D3 | The business processes return as **full ICM workspaces** (Van Clief grammar: numbered stages, CONTEXT.md contracts, review gates) — contracts + human gates, never a driver | 2026-08-26 | the 2026-08-12 factory retirement, narrowly |
| D4 | The whole repo is one five-layer tree: `sell`/`start`/`deliver` under `workspaces/`; the three rituals become deliver's stage contracts; slash commands survive as thin routers. **The stage contracts are the process** | 2026-08-26 | "the commands are the process" (2026-08-14 consolidation), in wording only |
| D5 | Deal artifacts are **tracked in git** at `workspaces/deals/<slug>/` (private repo; cloud sessions must run the pipelines; past deals are precedent). Durable docs copy into the client repo at kickoff; never a secret in a deal folder | 2026-08-26 | — |
| D6 | This repo holds the business **knowledge layer** (`_system/knowledge/`): services, pricing (fixed-price · retainer · in-kind; no day rate), voice, terms, stack — filled via `_system/setup/questionnaire.md`, honest gaps until then | 2026-08-26 | — |
| D7 | Estate Claude assets: **canonical library** in `_system/template/claude/` (session-start + wrap-reminder hooks, ticket-craft + pr-conventions skills); `icm-check.sh` seeds what's missing and reports drift, never overwrites — the repo's copy wins | 2026-08-26 | AUDIT open decisions #1 (skills layering) and #3 (hook strategy) |
| D8 | Client acquisition covers **inbound + outbound** (qualification, target profile, outreach playbook) — marketing/content stays with `jamienisbet` | 2026-08-26 | — |
| D9 | Proposals are **markdown → PDF** in the deal folder; markdown canonical, PDF a build artifact | 2026-08-26 | — |
| D10 | Tickets re-founded on the sustentus intake model: **epics + stubs + triage**, path identity (`epic/slug`), positional status. The `PREFIX-NNN` series and its dual registry are retired (legacy IDs live on in the archives); the today flag moves to `.icm/today.md` here, ≤10 entries estate-wide | 2026-08-28 | the 2026-08-12 TICKETS.md shape |
| D11 | D3 narrowed: a *project repo's own* deterministic one-job scripts, and CI acting only after a human-authorised merge, are **factory, not orchestrator**. The line that never moves: nothing advances work across a human gate, and icm-board itself drives nothing | 2026-08-28 | D3, narrowly |
| D12 | Gen-3 **is** a template product: the estate pipeline is extracted from sustentus into `_system/template/` as tiered profiles (`intake` default · `pipeline` · `pipeline-full` deliberately unextracted). Sustentus stays exempt as the source; remi-ai's zero-run `pipeline/` tree is superseded pending its migration decision. Answers AUDIT open question 2 | 2026-08-28 | — |
| D13 | The dashboard board stays **read-only** and reads both ticket shapes over the GitHub API, sustentus included — no sync, no second store | 2026-08-28 | — |
| D14 | **Clean slate**: every legacy `PREFIX-NNN` ticket — open and archived, estate-wide — is purged rather than re-cut ("they were getting very noisy"); fresh backlogs come from gated `/project` runs per repo, on demand. The "nothing is deleted" rule governs the new model's ongoing operation from this founding reset onward. Facts worth keeping (security lines, the hook-wiring history) were carried into AUDIT.md and the seeding stub before the purge | 2026-08-28 | the per-repo re-cut rollout in D10's plan |
| D15 | **Conformance exit codes**: gaps exit 0 (the report's content), unreachable stays exit 2 (the report lying) — in both `icm-check.sh` and `estate-conformance.sh`, commented at the exit lines. A always-red schedule teaches you to stop reading it | 2026-08-28 | the inherited `(( gaps == 0 ))` convention |
| D16 | The rails file is **`opencode.jsonc`**, not `opencode.json`. It keeps its `//` comment — the only record that bash permissions are last-match-wins, so the `claude/*` push allow must stay below the ask — and the extension declares that dialect to every other tool, so no repo needs a lint ignore **to parse it** and none is a latent break. (`cafe-jardim` still excludes it from Biome for *formatting*: that repo formats with tabs, and the canonical assets are two-space. A canonical asset is drift-checked byte-for-byte, so reformatting it in-repo trades a CI error for silent permanent drift. `.claude/settings.json` was flagged there too but is **not** drift-checked, so it was simply reformatted — see D17.) Rejected: a baseline `!**/opencode.json` ignore (reaches into each repo's lint config), and strict JSON with the doctrine moved to `AGENTS.md` (parts the warning from the two lines it is about) | 2026-09-04 | `opencode.json` as the canonical root asset (D7's set, epic opencode-sidecar) |
| D17 | A canonical asset that loses to a repo's formatter **stays a per-repo exclusion** — no estate machinery. Rejected: having `icm-check` parse each repo's lint config to report exclusions (a config-parser for a one-file problem), and seeding a template-derived ignore class into every repo (reaches into each repo's lint config, the same ground D16 rejected). The bar set in triage was a third repo or `.claude/settings.json` flagged beyond `cafe-jardim`; neither is met. The real surface is **two files** — `opencode.jsonc` and `skills/*/SKILL.md`; only the three `hooks/*.sh` are safe, being shell. (Surveyed as *one* file on the reading that the markdown passed Prettier: true of `remi-ai`'s `**/*.{ts,tsx,md}` glob, false of `prettier --check .`. Adopting `courseday` hours later put five files in scope and failed all five, `pr-conventions/SKILL.md` among them — k0d0minio/courseday#277. **Both revisit triggers this decision set have therefore fired**: the markdown assets collide, and `courseday` is the third colliding repo. The *ruling* held on contact — the peer session applied it as written and courseday went green in one commit, reaching independently for the same class-level `.icm/` + `.claude/` + rails exclusion `dungeons-dragons` wrote. Whether three repos converging on that by hand promoted it from per-repo call to seeded convention was carried in `triage/canonical-assets-markdown-collision.md` and answered **no** — see D19.) `.claude/settings.json` is seeded but never drift-compared, so there was never a trade there — `cafe-jardim` reformatted it to tabs. The same holds for `.icm/CONTEXT.md` and `.icm/intake/README.md`: seeded when missing, never compared (`icm-check.sh` runs exactly two `cmp -s`, over `.claude/`'s `CANONICAL` and `CANONICAL_ROOT`), so a formatter may have them freely. `collabimmo` carries a tab `biome.json` its CI does not run: latent, not a hit. The rule is written where someone seeding assets reads it (`_system/template/README.md`) | 2026-09-08 | — |
| D18 | **D7 narrowed one level down**: `--fix` may merge a missing hook registration into an existing `.claude/settings.json` — appending the template's own entry for a hook the repo's file does not already name anywhere, and only that. It never rewrites an entry the repo already has, never changes a value the repo already holds, and reads both the event and the entry from the template, so registering a future hook is a template edit and nothing more. Needs `jq`; without it the merge is skipped and the inert-hook warning stands, reason attached. The line D7 still draws: this is the only file `--fix` edits that it did not create, and canonical assets stay drift-reported, never repaired. Known cost, accepted: `jq` re-emits the document, so a repo that hand-packs an array onto one line gets it reflowed — `courseday` only, whose CI format-checks `.claude/`. Rejected: a one-time hand pass across the 14 (closes the backlog, leaves the next hook to repeat it) | 2026-09-08 | D7's “existing files are never touched”, for hook entries only |
| D19 | **D17 re-affirmed on the corrected surface**: a canonical asset that loses to a repo's formatter stays a **per-repo exclusion**, no estate machinery. Both triggers D17 set fired on `courseday`'s adoption — the markdown assets collide, and it is the third colliding repo (k0d0minio/courseday#277, five files in scope, all five failing) — but re-running the survey with the method fixed *narrowed* the case rather than confirming it. **Formatting the template clean is not available**: all four copies of `pr-conventions/SKILL.md` are byte-identical (`md5 20f6cfaf8031`), and those same bytes **pass** Prettier's defaults (`remi-ai`, green) while **failing** `courseday`'s (`printWidth: 100`, `semi: false`, `singleQuote: true`) — no single formatting satisfies Prettier defaults, courseday's config and cafe-jardim's tabs at once. **Seeding the exclusion has nothing left to protect**: of the eight repos carrying a formatter config, every one that invokes it over the canonical assets is already covered — `courseday`, `dungeons-dragons` and `cafe-jardim` by hand; `remi-ai` green on its `**/*.{ts,tsx,md}` glob; `escondidinho` runs `biome check` but Biome does not format markdown, so its only reachable asset is `opencode.jsonc` and its style agrees; `collabimmo` and `garmani` carry configs their CI never invokes (ESLint, and no `format` script). Zero repos are exposed, so `--fix` would seed ignore blocks only where they already exist — and doing it would mean parsing `.prettierignore` lines against `biome.json` `files.includes` negations (the config-parser D16 and D17 both rejected) and creating a `.prettierignore` where a repo has none, changing that repo's formatting well beyond the assets. **The trigger is spent one way and narrowed to one**: all 26 repos are adopted, and `createClientRepo` scaffolds the baseline with no formatter, so this can now only fire when an **already-adopted repo adds or changes a formatter config** — `garmani` and `collabimmo` are the two latent configs. Accepted cost: nobody will be watching when it does, so it is found by a red check; the three hand-written exclusions are the precedent to copy, each carrying its reason in a comment | 2026-09-09 | re-affirms D17 and closes its revisit; drops the duplicate stale D16 row |
| D20 | **File-level ownership for the pipeline profile, and the one sync.** The template's pipeline files are split in `_system/template/icm-pipeline/MANIFEST` into **template-owned** (`T`: the four stage contracts, the four lanes, `intake/CONTEXT.md`, `_shared/{github,ci,stage-preamble,scope-template,conventions}.md`, every factory script and `scripts/lib/`) and **project-owned** (`P`: `project.json`, `_shared/{project-rules,knowledge-map}.md`, `scripts/{format,lint,validate-knowledge-map,notify}.sh`, `runs/README.md`). `T` files are byte-identical in every pipeline repo — **sustentus included** — carry no repo's identity, and read what is repo-specific at runtime from the `P` files (`lib/project.sh` reads `project.json`; contracts point at `project-rules.md`). `icm-check.sh` reads the manifest, requires both sets, seeds `P` stubs once (filling `project.json`'s `name`), and **drift-reports** `T` files; `_system/scripts/icm-sync.sh --apply <repo>` is the one repair — human-invoked, dry-run by default, `rsync --files-from` the manifest so nothing else can move, refuses a repo that does not declare `- profile: pipeline` and a dirty `.icm/`, never deletes (a retired file such as `stages/01_scope/approve/` is reported for `git rm`). The `approve` substage is gone from the template and every place that routed it, as sustentus absorbed it on 2026-09-15: Scope settles the source **live in session**, emits a `D-n` decisions table, and `validate-decisions.sh` traces the ids into `spec.md` and `notes.md`. Narrows D7/D12: canonical `.claude/` assets and every `P` file keep "reported, never repaired"; sustentus stays exempt from the estate walk (`--repo` measures it voluntarily) and is the reference the template is re-founded from and the first sync target. Rejected: mustache placeholders in contracts (D12's "no substitutions" holds — identity is removed, not templated); an `--update` sync (transfers zero files to any repo that ever edited one — the dry-run gap analysis §2.2); an exclude-list sync (anything new in the template would propagate uninvited). Source: `ICM_Refactoring_Directive_v5.md` (Jamie), `ICM_DryRun_Gap_Analysis.md`, `ICM_Deep_Dive_Stage_Audit.md` | 2026-09-18 | D7's "never retro-syncs existing files" and D12's "sustentus stays exempt as the source", for `T` files only |
| D21 | **Local feedback scripts: project-owned, changed-files-only, feedback-only.** `scripts/format.sh` and `scripts/lint.sh` run the repo's *own* formatter and linter over exactly the files the branch changed (`lib/changed-files.sh`), invoked only as `.icm/scripts/<name>`, never with `--fix` for lint; **CI stays the verdict** and the full sweep stays blocked by `block-local-checks.sh` and every `opencode.jsonc`. The template ships them as `SKIP` stubs; a repo wires its tools into its own copy (sustentus's prettier/eslint pair, #1118, is the precedent). The directive's §2C "shift-left MUST execute locally" is adopted in exactly this narrowed form and no further: the GitHub Actions release gates and quality workflows are not moved local. Answers the deep-dive's E9 | 2026-09-18 | — |
| D22 | **One pipeline, weighted by complexity — the profile tiers are gone.** Every adopted repo is a pipeline repo: `icm-check.sh` requires (and with `--fix` seeds) the pipeline everywhere, and `icm-sync.sh` no longer reads a `- profile:` line — its guard is "a git repo carrying `.icm/CONTEXT.md`", plus the clean-`.icm/` and manifest guards D20 set, all unchanged. What replaces the tier is a **`complexity`** key in the project-owned `.icm/project.json` (`standard` default · `micro` for a one-page site or a script repo — the seeded `validate-knowledge-map.sh` returns 0 at once); an old `- profile:` line or `"profile"` key is ignored, never an error, so no repo has to be edited for this to hold. Three additions ride the same template, all `T`, all one-job factory scripts under D11 — none starts a stage or crosses a gate: **`select-model.sh`** (a stub's / scope's / spec's `complexity` → `sonnet` · `opus` · `fable`; an explicit `recommended-model` wins; it prints, it launches nothing), **`check-migrations.sh`** (Release step 7: after `main` is merged in, are this run's `YYYYMMDDHHMMSS_*.sql` migrations still newer than `main`'s — report by default, `--apply` renames and never commits), and **`process-raw.sh`** with `.icm/raw/` → `.icm/processed/` (local extractors only — nothing is uploaded to be read — a `manifest.json`, the original archived to `raw/_processed/`, and one **triage pointer stub** per asset that Scope retires). **Run-scoped isolation** is now stated as a rule in `_shared/stage-preamble.md` and restated in every stage's and lane's Outputs: a run writes only under `.icm/runs/<slug>/<stage>/`, on `claude/<slug>`, in a working tree no other live run shares; the front stays the one writer on `main`. Adopted from the directive **narrowed in four places**: (1) the fields are the estate's `- complexity:` / `- recommended-model:` header lines, not YAML front-matter — every parser and the board read dash-fields (the helper reads both); (2) `project.json` keeps `required_checks`, `personas` and the two archive keys — template-owned scripts read them — and `name` stays empty for `--fix` to fill, because D12/D20's "no placeholders" holds (`{{PROJECT_NAME}}` would pass the name check unfilled); (3) the ingestion script parks triage pointers and cuts **no** epic or sequenced stub — a stub `/pipeline new` would walk into Define without Scope having settled it is the one thing the pipeline forbids; (4) stub `complexity` (`low · medium · high · research`) sits beside the spec's `trivial · standard · complex`, which the labels depend on — mapped in `intake/CONTEXT.md`, both read by the helper. **Known cost, accepted:** the next estate `icm-check.sh` reports ~45 missing files in every repo that was `intake` (this one included — it is held to its own baseline), and `--fix` will seed them; that rollout is its own session, repo by repo, with the formatter guard `/project` § 1c already describes. Source: *Agent Directive — Central ICM Template Incremental Refinement & Sync Verification* (Jamie, 2026-09-21) | 2026-09-21 | D12's tiers (`intake` default · `pipeline`) and D20's "refuses a repo that does not declare `- profile: pipeline`" |
| D23 | **The agency layer, from the DevOps brief (r3, 2026-09-22).** Reporting is abstracted into **message kinds** a repo maps to whatever channels it has — `report.sh <announce\|alert\|economics>` (project-owned, complete as seeded, steered by `project.json → reporting`, exit 0 always) with a **GitHub Release on every merge by default**; Slack and email are added per repo by editing two arrays; `alert` mapped to nothing means the red CI job is the alert; `announce_from` decides the one caller (Release step 9 or the reference `release.yaml`). **Vercel is declared** in a `deploy` block (platform, team, the token's *name*, projects with class product/quiet) read by the new `lib/vercel.sh` (read verbs only), `deploy-status.sh` (Release step 9's one production read → the `- production:` line), `env-check.sh`, `ci-status.sh` and `rollback.sh`. **Environment** is a per-repo `env.sh` (audit · init · pull · push-notes · add · doc) over `.env.example` with two new suffix tokens `[ci]` and `[cloud]`, values on stdin only; `vercel-env.sh` becomes the estate loop (`--via-repos`) and regenerates its registry from the deploy blocks (`registry`); the cloud hydrate hook delegates to `env.sh pull`. **Recovery** is a human-invoked `hotfix` lane that opens READY plus `rollback.sh`, which prepares a revert PR and/or names the previous READY deployment and never executes either; stop class 3 is scoped to `migrations.reversible` and measured by `env.sh audit --changed`. **Usage** is one cumulative `- usage:` line per stage start/end in `runs/<slug>/usage.md` (`usage-snapshot.sh`, both harnesses read from their own stores, priced in-repo from the synced `lib/model-prices.json`, dated), rolled up per client by icm-board's `run-economics.sh` into the deal's `private/economics.md` (D24 amends the brief's path). **`/setup`** replaces `/project` § 1c: `setup.sh` (eleven sections, `RESULT: OK \| GAPS n`, `--fix` seeds from an explicit `--template` source and never overwrites; **no default source**, `SKIP template` without one) plus the `setup` skill beside the router; the MANIFEST becomes a T file in every repo with `.icm/template-version` beside it; `/icm-check` runs each repo's `setup.sh --report`. **Nothing a repo runs reads anything outside the repo** (flag 1); T files carry no identity; `notify.sh` is retired (reported for `git rm`). Rejected: a client-facing gate (the operator signs off on the client's behalf); a standing health-check workflow (one read at Release, decision 6); alert-carried commands into the hotfix lane; a relative default template path. Source: `.icm/docs/Agency_DevOps_Brainstorm.md` (r3) | 2026-09-22 | D20's "the MANIFEST stays in the template"; D5/D12's `notify.sh`-shaped announcing; `/project` § 1c (2026-09-18 run-log row) |
| D24 | **One home per fact; the deal workspace lives in icm-board.** Relationship state (rung, next action, stripe_customer_id, github_repo, work_started_at, the agreed value and shape) lives in Neon and is never mirrored into git — the Ladder, Stage and Value rows leave DEAL.md. Every deal document, private reasoning included, lives in icm-board at `workspaces/deals/<client>/<engagement>/`, committed straight to main with a `Deal:` prefix. A client repo receives only immutable, provenance-stamped snapshots at kickoff (`.icm/docs/proposal-<date>.md`, `scope-<date>.md`) and never anything under `private/`. The dashboard reads the deal folder live (read-only, D13) and shows a rung-versus-stage badge; nothing syncs. A copy is allowed only when immutable and provenance-stamped. Rejected: Neon as the document store (breaks the four ticket-storage constraints of 2026-08-30 and regrows the machinery the dashboard retired in August); a `.icm/client/` workspace per repo (splits one engagement across two repos, scatters precedent, and puts the relationship on a repo meant to be handed over). remi-ai and sustentus get tracked deal folders here. The client repo is created at signature by default; earlier on Jamie's request (supersedes the first-round "at first reply", whose reason moved). Source: the master directive, `.icm/docs/2026-09-22-rework-master-directive.md` §1 | 2026-09-22 | D5's "deal artifacts … copy into the client repo at kickoff" — only in *what* may be copied (immutable, provenance-stamped, never `private/`); the first-round "repo at first reply" |
| D25 | **Documents to Jamie's own Drive; sending stays human.** A deal session may render a proposal or agreement to DOCX and place it in Google Drive under a folder named after the client (creating the folder when absent) using the Drive connector, and log the link in DEAL.md. Sending for signature, and every message to the client, remain Jamie's. This narrows "no outbound action" the way D11 narrowed D3: a write to the operator's own storage is not an outbound action. Source: the master directive §1 | 2026-09-22 | D3/D11's "no outbound action" as read over writes into the operator's own Drive (a placement is not a send) |
| D26 | **Pick-up is the pipeline verb; runs are cut for disjointness.** The board's copy-prompt and deep links send `/pipeline new <epic>/<slug>` (or the lane verb for a triage stub) wherever the repo carries the `/pipeline` router, and the `## Prompt` body only where it does not. Scope groups stubs into `## Parallelizable` only when their `touches:` do not overlap; Build merges origin/main before the ready flip; `new-run.sh` warns on overlap with a live run. Report, never refuse. Source: the master directive §1 | 2026-09-22 | TICKETS.md's "`## Prompt` alone is the pick-up contract" |
| D27 | **The retrospective: a run's fixed errors become the next run's rules.** A stage records every error it fixes where it fixes it — `error.log` in its own output folder (`03_build/output/` on the spine, `lane/output/` in a lane): a dated `## ` header naming the source, the failing lines verbatim, a `- resolved:` line once the fix landed (written as a sentence the next run can act on), a `- rule:` line only for a constraint of that repo. `retrospective.sh` (template-owned, `T`) reads that file at Release step 7(b) and at the end of every lane, **before the close-out moves the run**: a signature per entry (the error class — a TS code, an ESLint rule id, a Biome rule, an errno, an exception class — never the instance), counted across the archive's `error.log`s and never a live sibling's; an entry flagged `- rule:`, or one whose signature recurs (`--min`, default 2) and carries a resolution, is a candidate; a signature already in `_shared/project-rules.md` is skipped. It reports; `--apply` appends each candidate under `## Learned rules` in the project-owned `project-rules.md` in the directive's shape (`<!-- Retrospective Learned Rule [date] -->` + one bullet carrying the signature, the count, the runs and the areas the diff touched) and never commits — the stage commits the lines with the run's record, so the operator reads the rule in the PR and the squash publishes it; a rule that reads as a slip is deleted by hand before that commit, and that edit is the whole editorial control. Build and every lane read `## Learned rules` in their Inputs with the code rules' standing — that read is what closes the loop. Adopted from the directive **narrowed in three places**: (1) the script never judges — the rule text is the session's own `- rule:` / `- resolved:` words written at the moment of the fix, because a shell script cannot tell a constraint from a slip and an LLM call inside a factory script would be the orchestrator D3 forbids; (2) it reports by default and `--apply` is the stage's explicit act, the same standing as `check-migrations.sh --apply` and `close-out.sh` (D11: a one-job script whose output a human merges); (3) the HTML comment is a provenance stamp in a project-owned file the sync never reads, not the intra-file merge marker D20 rejects. The directive's `error.log` had no writer in the pipeline (v5 named it; nothing wrote it) — Build step 4 and every lane's RED path now do, by hand, at the moment of the fix. Rejected: an automatic append at close-out with no report step (a rule nobody read would bind the next run); reading CI logs or the transcript directly (the session already read them — what it wrote down is the record); a rules file outside `project-rules.md` (one home for what is true of the repo). Source: *Agent Directive — Continuous Learning Engine & Retrospective Collector* (Jamie, 2026-09-23; §1 received), saved as `.icm/docs/2026-09-23-retrospective-directive.md` | 2026-09-23 | — |
| D28 | **The deal folder is named after the repo.** `workspaces/deals/<slug>/` = the client repo's name, not the person's: one string names the relationship in both homes, so the dashboard finds the folder from `github_repo` and `deal_slug` is retired (its column goes with a migration in `jamienisbet`, Jamie's to cut there). A person with two businesses has two repos and two folders — a deal per business, not per person. Five folders renamed by `git mv` with a log line each (alix-hahusseau→berceo, billy-carlson→vinecliff, diogo-rita→agorasim, rui-matias→kau-american-bbq, remi→remi-ai). **One-off, not a rule change:** five closed relationships (karen, magali, jerome, dragon, alex-valexo) were removed from `workspaces/deals/` at Jamie's direction — git history keeps them; README § Rules "Nothing is deleted" stands. Dated records under `.icm/docs/` keep the old slugs. Source: the deal-folder sweep, Jamie 2026-09-23 | 2026-09-23 | — |
| D29 | **Five capabilities from the enhancements directive (2026-09-23), narrowed to the ICM ethos.** (1) **Capability skills** live at `.icm/skills/<name>/SKILL.md` in three tiers — front matter (`name`, `description`, `triggers`; ≤80 tokens, `list-skills.sh --check` says), a body loaded only on a trigger, `references/` + `scripts/` on demand — and `list-skills.sh` prints the Level-1 registry, which the canonical `session-start.sh` injects; three seeded, template-owned (`security-audit`, `database-migration`, `preview-deploy`), each wrapping the template's own scripts and carrying no repo identity. They are *procedures a stage follows*, distinct from the harness skills under `.claude/skills/` that route and enforce. (2) **Advisor–executor routing** is a lookup, never a dispatcher: `select-model.sh --stage <s> [--complexity <w>]` maps role × complexity → tier → alias and `flags: --model <alias>` — Scope and Define the advisor (tier 3: `opus`, `fable` on research), Build, Release, every lane and any subagent the executor (tier 2: `sonnet`, `opus` only on `high`), a lint/format fix the validator (tier 1: `haiku`, never overridden by a stub's `recommended-model`); without `--stage` the D22 mapping is unchanged. The operator opens sessions; the contracts say the recommendation aloud once and carry on. (3) **One database per run**: `db-branch.sh <slug> status|up|env|down` binds a live run to a Postgres schema `run_<slug>` or a local container `icm-db-<slug>`, declared per repo in `project.json → database.isolation` (`none` default — SKIP, migrations stay the preview's and CI's); `env` prints only exports, the variable's *name* goes in `run.md`, no value touches disk. **Millisecond stamps are enforced on this branch's own migrations only**: `V<17 digits>__<name>.sql` (`migrations.stamp: millis`, the default; `seconds` keeps the legacy `<14>_<name>.sql`; both forms are always read and ordered by stamp value) — `check-migrations.sh --new <name>` names a file, `MISNAMED` renames with the stamp kept, `STALE` re-stamps as before, and the tool's out-of-order setting (`migrations.tool`, `migrations.out_of_order`) is *printed*, never written into a tool's config. (4) **A zero-trust gate that is local on purpose**: `security-check.sh` before every Build commit and every lane push — `gitleaks` over the staged change (the directive's `protect --staged --redact`), a built-in fifteen-pattern fallback that says aloud it is the fallback (never silent when gitleaks is absent; `--strict` fails instead), a real `.env*` in the change set as a finding, `npm|pnpm|yarn audit --audit-level=high` (or `security.audit_command`) only when a manifest moved on the staged scope, always on `--branch`/`--all`; findings redacted, appended to the run's `error.log` as an entry in D27's shape (so `retrospective.sh` reads it), exit 1. The one exception to "CI is the verdict": a secret must never reach the remote at all. (5) **The canonical file pack** — `project.md`, `plan.md`, `tasks.md`, `decisions.md`, `status.md`, `handoff.md`, `FAILURE.md` — seeded into every run by `new-run.sh` (through `run-pack.sh --init`, from `_shared/run-pack/`, never overwritten; the spec's criteria become the DoD, the scope's `D-n` rows the ledger), read by the preamble (`status.md`, then `handoff.md`) before any contract, rewritten at every stop; `close-out.sh` copies `FAILURE.md → Learned rules` into `_shared/project-rules.md` (append-only, deduplicated, in the shape and section D27's `retrospective.sh` appends to) in the archive commit — a write outside the run folder named in the preamble's rule 5. The two writers split one job: D27's collector learns from `error.log` (the errors a tool reported, counted across the archive), this sync from `FAILURE.md` (what no tool logged — a wrong assumption, a STOP, a rewritten plan). **Rejected**: LSFS / vector indexes (the directive's own exclusion — everything is `find`/`grep`/`jq`); a script that switches models or dispatches subagents (D3); auto-editing Flyway/Prisma/Drizzle config; a gate that passes quietly when its scanner is missing; making `millis` retroactive on `main`'s migrations (only a branch's own are judged, so no repo breaks on sync). **Known cost**: the canonical `session-start.sh` and the T contracts changed, so every pipeline repo drift-reports until `icm-sync.sh --apply`; `check-migrations.sh` will report `MISNAMED` on any branch that names a new migration in the legacy form after the sync — the fix is `--apply`, or `stamp: seconds` in that repo's `project.json`. Source: `.icm/docs/2026-09-23-icm-system-enhancements-directive.md` (Jamie); verification in `.icm/docs/2026-09-23-icm-system-enhancements-verification.md` | 2026-09-23 | D22's `select-model.sh` mapping (extended, not replaced) and `check-migrations.sh`'s 14-digit-only reading; D20's MANIFEST (15 T files added); D7's `session-start.sh` bytes; extends D27's `## Learned rules` (a second writer, one shape) |
| D30 | **The health probe and its endpoint, from the pre-flight directive (2026-09-23); the directive's security gate is D29's.** `health-check.sh` (template-owned, one-job under D11): Release step 9(a) after `deploy-status.sh` — one GET per `health_endpoint` (`project.json`, top-level or per deploy project, read by `lib/project.sh`), 200 expected, three attempts with exponential backoff; a failure calls `report.sh alert` and parks **one `lane: bug` · `complexity: high` triage stub per merge SHA, written and never committed**; `FAIL` exit 3. `/setup` asks for the endpoint (one per repo, or per deploy project) and `setup.sh` keeps it a `[WARN]` for as long as a repo deploys somewhere without one — asked once per repo, kept in its project-owned `project.json`; `env-check.sh` step 9 reports the audit tool and a declared endpoint. The directive's `security-check.sh` was built on this branch too (an added-lines scan, cargo/pip audits, one `error.log` entry per finding) and **withdrawn at the merge**: D29 landed the same gate first and wired it deeper — staged/branch/tree scopes, the `security-audit` skill, the pre-commit hook, `security.audit_command` for other ecosystems — and one script has one home. What this branch keeps of it, on Jamie's ruling against the first cut's dependency-change gating: Release step 4 reads the gate over the whole branch **with the audit on every read** (`--branch --audit`), and a pre-existing advisory that cannot be bumped on the branch is the operator's recorded waiver in the `## Release` record (`--branch --no-audit`), never the stage's. Adopted from the directive **narrowed in three places**: (1) `notify.sh` is retired (D23) — the alert is `report.sh alert`, so the repo's `reporting.alert` mapping decides where it lands and "no channel" stays the seeded default; (2) the stub is a bug-lane pointer, never a hotfix and never a rollback — nothing parks a stub for the hotfix lane, `rollback.sh` prepares, the operator acts; (3) one bounded read at Release, not a standing workflow — D23's rejection holds. Known cost, accepted: one more T file every pipeline repo lacks until `icm-sync.sh --apply` (`triage/sync-security-gate-and-health-probe`, riding D29's sync). Source: *Agent Directive — Security Audit Gate & Post-Deploy Health Verification* (Jamie, 2026-09-23) | 2026-09-23 | — |
| D31 | **A client UAT environment is optional and persistent; the client's status report is everywhere.** `/setup` alone declares `uat: {branch, url}` in a repo's `project.json` (the seeded stub is empty; nothing else ever fills it). Once declared, every run's PR — spine and lane — targets the long-lived UAT branch (`lib/project.sh → pipeline_base_branch`; a hotfix still `main`), `close-out.sh` appends the run to `.icm/uat/batch.json` on that branch, Release reads the UAT deployment once (`deploy-status.sh --uat`) and announces nothing, and production is one promotion PR per batch: the client tests the batch as a whole at one fixed address; the operator records the client's word with `promote-uat.sh approve --by "<who>"` (the agency brief's rule 1 stands — no client-facing gate, and an agent never infers an approval), which opens a ready `type:promote` lane PR into `main` through `new-run.sh`; the operator merges it from GitHub; `promote-uat.sh sync` brings `main` back into the UAT branch, resets the batch and announces the release. One branch and one address, never an environment or a URL per batch; branch and address live in `project.json`, the batch's state in `batch.json` (D24); the reference release workflow announces default-branch merges only. `client-status.sh` is template-owned and needs no UAT: it compiles `.icm/output/client-status-latest.md` from `origin/main` (and the batch from the UAT branch) in the client's words, and `/pipeline status` runs it. Rejected: a per-batch preview environment (nothing constant for the client to bookmark); an approval an agent records from a message it read (a gate ticked by an agent); a script that merges the promotion (no pipeline script merges) | 2026-09-23 | — |
| D32 | **The database branches with the code: a Neon branch per environment and per run.** Where a repo declares `database.provider: neon` (the project id and the key's NAME in `database.neon`; `/setup` asks), three things hold. (1) **Previews** — `neon.previews: vercel` says the Vercel integration (either kind) creates `preview/<git-branch>` for every preview deployment and injects its variables at deploy time; the toggle is an operator act `db-env.sh init` lists, never a script's, and the reference `neon-cleanup.yaml` (seeded once by `/setup`) deletes the PR's branches on close because the Vercel-managed integration otherwise waits for deployment retention (months). (2) **UAT** — with D31's `uat.branch`, the UAT git branch's database is the integration's own `preview/<uat.branch>`: born from production on the branch's first deployment, persistent, nothing per batch, named by `lib/project.sh → neon_uat_branch`; `db-env.sh reset-uat --apply` resets it from production on the operator's call (`promote-uat.sh sync` names it after a promotion). (3) **Runs** — `database.isolation: neon` gives `db-branch.sh` a third engine, `run/<slug>`, a child of PRODUCTION with a 7-day expiry Neon enforces itself, through `lib/neon.sh` (curl + the key; no psql, no docker), under the same `up/env/down` contract. Run branches are never children of the UAT branch, so a UAT reset is never blocked by a run. Migrations reach a preview's and UAT's database at build — the repo's build command runs the migrate step, an operator act recorded in `project-rules.md`; production keeps whatever the repo does today. The production branch is never written, deleted or reset by any script; `lib/neon.sh` refuses every name the pipeline did not give; the key's value never reaches argv, stdout or git. Rejected: a script that flips the integration's toggle or sets the build command (Vercel is the operator's, D31); a pipeline-created UAT branch (the integration already makes one — two owners of one branch is drift); parenting run branches under UAT (blocks the reset, and previews parent from production anyway); a third-party delete action in the workflow (three curl lines under the pipeline's own transport rules do the same) | 2026-09-23 | — |
| D33 | **A template-owned file is changed at its source: a request in a repo becomes a prompt for icm-board, never an edit.** In every pipeline repo, a request to change a `T` line of `.icm/MANIFEST` — a stage or lane contract, a `_shared/` doctrine file, a factory script, a capability skill — or a canonical `.claude/` asset (the router, `/setup`, `pr-conventions`, `ticket-craft`, the hooks) defaults to a **template change request**: the session says the file is template-owned, writes a self-contained prompt for an icm-board session (the file by template path, today's lines quoted, the change in full, the evidence — slug, step, SHA, `error.log` entry — and the repo), parks it as one `found-by: template-change` triage stub whose `## Prompt` is the request (a pointer no lane consumes; the board's "Copy prompt" hands it over), shows it whole and carries on under the file as it is, the fault recorded in the run's `FAILURE.md` or `error.log`. icm-board changes the template on a `claude/` PR; `icm-sync.sh --apply <repo>` brings a `T` file back (a `.claude/` asset by hand, drift-reported until it lands); the stub retires in that commit with a `- superseded-by:` line. The one override is the operator's word spoken in the session ("patch it here now"): the copy is edited, the commit is a named `Patch:` of a template-owned file, the request is written all the same — the next sync overwrites the patch. A repo-owned addition outside the manifest is neither (D20's "in one repo only"). Home: `_shared/template-change.md` (T) — the check (`grep -E "^T <path>$" .icm/MANIFEST`), the procedure, the override, the prompt shape — pointed at from the MANIFEST header, the router (step 9, the lane-argument resolver, the help, the extension note), `/setup` step 2, the stage preamble's sixth isolation rule, the three lanes' first step, the intake contract's writers and `.icm/CONTEXT.md`'s travelling rules; on this side PIPELINE.md, the template README, the house doctrine, the routing table and the conformance stage. Rejected: a hook or script that refuses the edit (an orchestrator's move, and it would refuse the operator's override too — the guard is words, read where the work happens); the stub cut in icm-board from the repo's session (D23 — nothing a repo runs reaches icm-board, and a cloud session has no icm-board on disk); patching in place and "syncing later" (every other repo keeps the fault meanwhile, and the patch is lost at the next sync). Source: Jamie, 2026-09-23 — "a final guard", the template having run smoothly on berceo, agorasim and remi-ai | 2026-09-23 | — |
| D34 | **The migration checker reads the epoch-millisecond form: a MongoDB repo orders its migrations like a SQL one.** `check-migrations.sh` reads a third naming form beside Flyway's `V<17 digits>__<name>.sql` and the legacy `<14 digits>_<name>.sql`: `<13 digits>-<name>.<ext>` — the epoch-millisecond stamp ts-migrate-mongoose, migrate-mongo and their kin write — declared per repo as `migrations.stamp: "epoch"` with `migrations.extension` (default `sql`; `ts` for a TypeScript runner) and `migrations.tool: "mongodb"` (`lib/project.sh` → `migrations_extension`). All three forms are always read and ordered by stamp value (every form converted to the one 17-digit UTC stamp first); the declared form is what this branch's own must carry; `--new` names a kebab-case file in it with the form's extension; STALE re-stamps on the millisecond grid, MISNAMED keeps the tail. The tool note for mongodb says what the runner does: applied files are recorded by name, every unrecorded file is applied in stamp order (out-of-order is what it does), and a re-stamped file is a new name whose old record the runner prunes — so a migration must be idempotent to survive its own re-stamp, which a shared preview database already demands. The epoch form is read with the declared extension only, so a runner's `template.ts` beside the migrations is never one. No new engine for `db-branch.sh`: a MongoDB repo stays `isolation: none` and its preview database and CI apply the migration. Rejected: treating epoch-stamped folders as "not this script's" (the SKIP the header used to promise — a MongoDB repo would then carry the checker and gain nothing); a per-tool subclass of the script (one script, three regexes). Source: Jamie, 2026-09-23 — sustentus's 93 migrations are `<13>-<name>.ts` on ts-migrate-mongoose, and its sync would otherwise have declared no migrations path at all | 2026-09-23 | — |
| D35 | **A MongoDB repo gets what a Neon repo gets: a database per run, a database per preview, pruning, and migrations proven before merge — D32's sibling on one cluster.** Where a repo declares `database.provider: mongodb` (names only, in `database.mongodb`; `/setup` asks, never for a URI), every pipeline database lives on the ONE cluster the repo already uses, beside its two long-lived ones (`production_name`, `preview_name` — never dropped or reset; every drop refuses them by name). (1) **Runs** — `database.isolation: database` gives `db-branch.sh` a fourth engine under the same `up/env/down` contract: `run_<slug>`, made by the repo's own `seed_command` then `migrate_command up` with the name variable (`mongodb.name_env`, default `MONGODB_DATABASE_NAME`) pointed at it; `env` prints that one export; `down` drops only `run_*`. Before a database is created the cluster's caps (`mongodb.limits`, default the shared Atlas tiers' 100 databases / 500 collections; 0 uncapped) are checked and the pipeline's databases counted. (2) **The migration proof** — `db-branch.sh <slug> prove`, read at Build step 10 and Release step 7 after `check-migrations.sh`: on a fresh `run_<slug>` at the base's shape, this branch's own migrations up → down → up, the collection list and every index spec compared — `down` must exist and restore them where `migrations.reversible: true`, the second `up` must reproduce the first, and an `up` after the runner's records are forgotten (a re-stamp, replayed) must change nothing: the idempotency D34 already assumed, now measured. `UNPROVEN n` is stop class 3. (3) **Previews** — `mongodb.previews: branch`: the app derives `preview_<branch>` at runtime (`VERCEL_ENV === 'preview'` and `MONGODB_PREVIEW_PER_BRANCH=1` → `preview_` + the normalised `VERCEL_GIT_COMMIT_REF`; otherwise the name variable, as before) — no Vercel API write, nothing per branch; Vercel's system variables exposed and one flag on the Preview target are the operator's acts `db-env.sh init` lists, and unsetting the flag is the whole revert. The repo's preview-migrate workflow seeds and migrates `preview_<branch>` on each PR push and the smoke check waits for it; the reference `mongodb-cleanup.yaml` (seeded once) drops it, and a forgotten `run_<slug>`, when the PR closes. With UAT declared, the UAT database is `preview_<uat.branch>`; `reset-uat --apply` drops and re-seeds it — there is no copy of production, by design. (4) **Housekeeping** — `db-env.sh status` lists the databases by role against the caps; `prune --apply` drops `run_*` whose run is archived and `preview_*` whose branch is gone. **One rule, one home:** `lib/db-name.mjs` (T, dependency-free) is the name derivation the app imports and every script and workflow runs — lower-case, `[^a-z0-9_]+` → `_`, 63 bytes with an FNV-1a suffix past that — so the two cannot disagree; `lib/mongo.mjs` (T) is the transport on the repo's own driver (`mongodb`, or `mongoose.mongo`), found from the migrations path, the URI read from the variable and never printed. **Seeding is the repo's**: the template calls `seed_command` and nothing else, and clones no production data. Rejected: per-branch Vercel environment variables or an API write per branch (a writer per branch where a derivation needs none); a new cluster or URI for the pipeline (the adopter's one cluster already has room, and a second is a second secret); a mongosh or new binary (the repo already carries the driver); cloning production into previews or runs (the adopter's rule: its seed, unchanged); a separate prove script (the proof needs the run binding, the guards and the name rule `db-branch.sh` already holds). Source: Jamie, 2026-09-23 — sustentus the first adopter, its `db-migrate.yaml` serialising every PR through one shared preview database and its header saying "forward-only" while `project.json` says `reversible: true` — the proof settles which | 2026-09-23 | D34, narrowly (its "no new engine for `db-branch.sh`" clause) |
| D36 | **Production stays off the pipeline's cluster, and a preview may start as a copy of the shared preview database.** D35 put every pipeline database beside production on one cluster. But a user that creates `run_*`/`preview_*` databases needs `readWriteAnyDatabase`, and MongoDB roles have no prefix scope and no deny, so any credential a preview, PR job or developer machine holds could write production. So: (1) **non-production gets its own cluster**. Production keeps its cluster, alone, behind a user limited to `production_name`. The shared preview database, every `preview_<branch>` and every `run_<slug>` live on the non-production cluster; `url_env`, the Preview target and the preview CI secret point there; `db-env.sh status` then reads production "absent", by design. A second free Atlas tier (one M0 per Atlas project) costs nothing. A Vercel storage integration writes one variable across every environment with a user that reaches every database, so the repo disconnects it and sets the two URIs by hand. (2) **A per-branch preview database may be made as a copy of the shared preview database** on its first push (mongodump \| mongorestore `--nsFrom/--nsTo`, same cluster), where the repo's seed creates no tenant or login. The shared database is then migrated on each merge, so every copy starts at main's shape. It is still never a copy of production. (3) Vercel exposes its system variables by default, so `db-env.sh init` no longer asks for a toggle. The workflows read the per-branch switch as a repository variable of the flag's name, beside the Preview target's. Rejected: one cluster with scoped roles (impossible: no prefix scope, no deny); a per-branch Atlas role update (a writer per branch, which D35 already rejected); scripting the demo runbook into every preview (a Clerk round trip per preview, and far more code than a copy); cloning production (still rejected). Source: Jamie, 2026-09-23, on sustentus: its one `Vercel-Admin-sustentus` user served production and every preview, and its seed creates no tenant, so a seeded preview opens on nothing and the demo-tenant migrations fail | 2026-09-23 | D35, narrowly (its "one cluster" and "clones no data" clauses) |
| D37 | **A pipeline database name fits the cluster it lands on: the cap is `database.mongodb.limits.name_bytes`, default 38.** D35 capped names at MongoDB's 63 bytes, but that holds only on a dedicated (M10+) cluster; Atlas's shared and free tiers (M0/M2/M5) — where D36 put every `preview_<branch>` and `run_<slug>` — refuse a name over 38. So `lib/db-name.mjs` takes the cap as a parameter (`pipelineDbName`/`runDbName`/`previewDbName`/`databaseName`, default 38, 20–63 or an error), and a name over it keeps its first cap − 9 characters plus `_` + the FNV-1a hash, as before. **The CLI reads the cap from `.icm/project.json` itself** (`process.getBuiltinModule`, Node ≥ 20.16 — no import the app's bundler would see), so every script and workflow that runs it agrees without threading a value; `lib/mongo.mjs` passes it too. The app cannot read `project.json`: a repo that declares anything but 38 passes the same number as `databaseName()`'s third argument, and `db-env.sh init` prints the line with it. Rejected: 63 by default (breaks every adopter so far on the first branch over ~30 characters); an environment variable for the cap (a second home the workflows would each have to set); reading `project.json` from the imported module (`node:fs` in the app bundle). Source: sustentus/sustentus#1150, `claude/sentry-web-instrumentation` → `preview_claude_sentry_web_instrumentation` (41 bytes) refused by `sustentus-staging` (M0), 2026-09-23 | 2026-09-23 | D35, narrowly (its "63 bytes" clause) |
| D38 | **A client repo's ticket state has one home — its ticket base branch — and every writer reaches it through a PR.** On a repo that declares `uat` (D31) a stub was born on `main` (Scope's front, `/day`, `/project` and a parked template change pushed straight there) and died on `uat` (`close-out.sh` moves it inside a run PR that merges into `uat`), while every reader — the dashboard (`git/trees/HEAD`), `tickets-board.sh`, `ticket-hygiene.sh`, `client-status.sh` — reads `main`: a finished stub read as open until promotion, could be picked into `today.md`, and could be run twice. So: (1) **the ticket base branch** is `uat.branch` where `.icm/project.json` declares one, else `main` — exactly what `lib/project.sh → pipeline_base_branch` already answers, reused rather than restated (D24). Every reader reads it; `main` on a UAT repo carries a lagging copy only promotions update, and nothing reads that copy for tickets. (2) **The ticket PR** is the one shape every writer outside a run uses: branch `claude/tickets-<topic>-<YYYYMMDD>`, title `Plan: …` / `Wrap: …` / `Scope: <slug> — intake cut`, label `type:tickets`, a body carrying `- announce: none` and `- audience: internal` (so a release workflow never announces it), and a **path guard** — `.icm/intake/**`, plus `.icm/runs/<slug>/**` for Scope; anything else is not a ticket PR. (3) **Merging is publishing.** The session that opened the ticket PR verifies the path guard (`git diff --name-only origin/<base>...HEAD`) and then **merges it at once** (squash), waiting for no check — the diff is ticket markdown, there is nothing for a check to catch; where a ruleset requires checks the merge takes the admin bypass (`gh pr merge --squash --admin`), which is why that bypass stays. A PR that no longer merges cleanly is rebased on its base and retried once, then reported. It is **the one PR an agent merges**; code, lane and promotion PRs stay the operator's, and GitHub auto-merge is not used. (4) **Scope's front is a ticket PR** into the ticket base branch — PR regime 1 reversed: no direct push, no bypass-list identity; the stub exists for `new` once it merges, and the operator still reviews the scope before `new`. (5) **Hotfix and the knowledge lane stay on `main`**; their merges reach the ticket base branch only through `promote-uat.sh sync`, now **required** after both. (6) Stubs a run parks or consumes still ride the run's own PR, which already targets the base branch. (7) **icm-board is exempt**: it has no UAT branch and nothing to drift, so its `Plan:`/`Wrap:`/`Deal:` commits and `today.md` stay direct to `main`. `pr-conventions` holds the ticket PR shape and the merge rule once; everything else points there. Out of scope: estate plumbing fan-outs straight to `main` (2026-09-07 ruling) — they carry no ticket state. Rejected: GitHub auto-merge (not on the free plan for private repos); waiting for Vercel on a markdown diff; a second, `main`-side ticket home kept in step by a script. Words here; the scripts, the board and the rollout follow (`.icm/intake/ticket-base-branch/`). Source: Jamie, 2026-09-23, from a finished stub still open on a UAT repo | 2026-09-23 | D31 and PR regime 1 (`_shared/github.md`), narrowly; "ticket-only commits go straight to `main`" for client repos |
| D39 | **One branch, two deployment targets: UAT is a Vercel custom environment deployed from `main`, production is a promoted staged deployment, and a published GitHub Release is the record of the client's word.** The long-lived UAT branch (D31) put UAT permanently *ahead* of `main`, and every rule since paid for the gap: a ticket born on one branch and dying on another (D38), `promote-uat.sh sync` "required" after every hotfix and knowledge merge, `main` carrying a lagging copy nothing may read, two repos on one shape and four on another. One day after D38 shipped, agorasim's `uat` sat 9 commits ahead of `main` while `main` held a knowledge-lane merge `uat` had never received. So: (1) **`main` is the only long-lived branch in every repo.** Every run, lane, hotfix, knowledge change and ticket commit targets it; `lib/project.sh → pipeline_base_branch` and `lib/ticket-base.sh`'s branch lookup go, and every caller reads `main`. (2) **UAT is a deployment target, not a branch.** `/setup` alone declares `uat: {target, url}` in `project.json` (the seeded stub stays empty): `target` is the slug of a Vercel **custom environment** on the repo's project — Pro, one per project; both estate teams are Pro with the slot unused; where a team cannot create one, `/setup` refuses the declaration and UAT stays undeclared — deployed from `main` on every merge (branch tracking where Vercel accepts it on the production branch, else the reference workflow's `vercel deploy --target`), with its own variables, and `url` its attached domain: one address, always on, the whole batch integrated, as D31 asked. Inside it `VERCEL_ENV` is `preview` and `VERCEL_TARGET_ENV` is the slug, so a build that migrates its preview database migrates UAT's exactly as today. (3) **The UAT database is a named branch, not the integration's.** `database.neon.uat_branch` (the MongoDB name likewise) is declared by `/setup` and its connection set on the custom environment's variables by the operator — a persistent child of production; `db-env.sh reset-uat` unchanged; the Neon integration is not connected to the custom environment. D32's run and preview rules stand. (4) **Production does not follow `main`.** On a UAT repo the operator turns off **Auto-assign Custom Production Domains** (Vercel → Settings → Environments → Production → Branch Tracking; all plans): every merge still builds a production deployment, held **Staged**; the production domains move only when one is promoted, and a promotion never rebuilds. (5) **The client's word is a GitHub Release, and publishing it is the promotion.** `promote.sh approve --by <who> [--note]` records the word by *drafting* a Release at the signed-off `main` SHA — body: who, when, the note, `announce`, the stubs since the last published release; **the operator publishes it on GitHub** (no client-facing gate; no agent infers an approval — the agency brief's rule 1 stands). The reference `release.yaml`, on `release: published`, runs the repo's production migration — the reference `db-migrate.yml` moves from `push: main` to this event: migrate, verify, *then* promote; a failed migration stops before the promote and production is untouched — promotes the staged deployment of that SHA (`POST /v10/projects/{id}/promote/{deploymentId}`; already Current → a no-op, so a baseline release and a re-run are safe), reads the production deployment once and announces. Rollback is Vercel's instant rollback (`rollback.sh`); a Release is never unpublished. On a repo without UAT nothing changes: the merge is the promotion and the workflow announces it as today. (6) **The batch is `git log <last published release>..origin/main`.** No `.icm/uat/batch.json`, no `sync`, nothing to reset. `client-status.sh` reads *live* (at or before the release), *on UAT, awaiting your word* (merged since), *in progress* and *queued* from `origin/main` and the repo's Releases; `promote.sh status` shows the same plus the staged deployment for `HEAD` and the UAT deployment. (7) **A hotfix promotes everything before it.** With one branch there is no shipping a fix around unsigned work, so the rule moves upstream: **a run not fit for production does not merge, or merges dark** (a flag, an unlinked route); batches stay small and promote often; the hotfix lane keeps its name for its urgency, targets `main` like everything else and is promoted at once. (8) **Ticket state has one home, `main`, reached by a direct commit** — the comfort every repo had before D38, back for every repo. `Plan:`/`Wrap:`/`Scope:` cuts and stub moves push straight to `main` in icm-board and client repos alike; the ticket PR shape, `--admin` merging and `type:tickets` skipping are retired (the label may stay, unused); berceo's merge-gate ruleset keeps its admin bypass so the operator's own push lands; agorasim, private on GitHub Free, has no rulesets to bypass. The dashboard's `git/trees/HEAD`, the board and hygiene read `main` and are right again. (9) **Clean cut.** `uat.branch` leaves the schema and `setup.sh` fails a repo still declaring one; `uat/CONTEXT.md`, `promote-uat.sh` and `batch.json` go; `_shared/promotion.md` (T) and `scripts/promote.sh {init,status,approve}` replace them; the operator's Vercel acts stay the operator's, listed by `promote.sh init` and never performed by a script (D31, D32). Rejected: switching database by hostname inside one deployment (build-time variables, migrations and caches disagree, and one routing bug leaks production data into UAT); promoting the UAT deployment itself (Vercel rebuilds a promoted preview with production variables — the client signs off one build and receives another); a second Vercel project as the UAT target (works on any plan; a second shape to keep in step — refused with the Hobby case); a hotfix branch off the release tag as tooling (a second path is the drift returning; an operator may still cherry-pick by hand, undocumented on purpose); `approve` promoting through the API directly (a pipeline script performing the production act; record and state could disagree); keeping the branch model behind a flag until both repos are over (exactly the per-repo divergence this decision removes); expand-only migrations on push (production schema ahead of production code — a discipline a solo shop should not have to hold). Words here; the template, icm-board's scripts, the two-repo cutover and the estate sync follow (`.icm/intake/one-branch-two-targets/`). Source: Jamie, 2026-09-24 — "the whole issue with the uat setup is that it leaves a lot of room for discrepancies"; two rounds of questions the same session, eight answers: draft release the operator publishes; migrations at promotion; a hotfix promotes everything; direct ticket commits; the operator's Vercel acts from a checklist before any merge; UAT requires Pro; clean cut; serviflow set aside for now | 2026-09-24 | D31 (the branch, the batch file, `sync`, the promotion PR — the client's one fixed address and the batch sign-off survive), D32 clause (2) (the UAT database is a named branch, never `preview/<uat.branch>`), D38 (1)–(4) and the premise of (7) (`main` is every repo's ticket base, reached by a direct commit — icm-board's exemption becomes the rule) |
| D40 | **One output doctrine, one stop-report shape: `_shared/output.md`.** Nothing in the template said how much a session says or in what shape — each of the ten stop steps (the four stages, the six lanes) improvised its own sign-off, and narration between tool calls was unbounded, so the lines Jamie must act on were buried in prose the run's own files already carried. So: a new template-owned `_shared/output.md` (T, under the MANIFEST) states four things. (1) **Audience** — the chat is the one surface the operator reads; an agent or a cloud session picking up later reads files, never chat, so anything a later session needs is written to a file (`handoff.md`, `status.md`, `notes.md`, the PR body) and never only said. (2) **While working** — one line per phase change, no narration of a tool call, no restating a file, a diff or a command's output the files already carry. (3) **At a stop, one shape everywhere**: `<stage/lane> <outcome> · CI <verdict> · <PR link>`, an `Operator:` checklist of `- [ ]` human-only acts, and `Unverified:` only when non-empty — no recap of what changed, since the PR body and `notes.md` carry that. (4) **Split by actor** — `handoff.md` is the next session's steps; `Operator:` is human-only acts that never land in git (a gate tick, a merge, a Vercel dashboard or env change, a secret rotation, a DNS record), in plain words the act is doable from alone, no decision number without its meaning; no fact lives in both, except an operator act that *blocks the run*, which is also a `handoff.md` → Blockers line (`blocked on operator: <act>`) because a chat-only list dies with the session. **Never trimmed, regardless of the line budget**: a STOP and its reason, a red check, anything skipped or unverified, a plaintext credential found — brevity never outranks reporting outcomes faithfully. **Scope**: the pipeline (stages, lanes, `/pipeline`, `/setup`) and the four canonical `.claude` skills (`pr-conventions`, `ticket-craft`) that sync into every estate repo — not icm-board's own commands (`/client`, `/project`, `/day`, `/icm-check`), not the global `~/.claude/CLAUDE.md`. **Enforcement is one file, cited by every stop step** — no Claude Code output style (no OpenCode twin to keep in step) and no Stop-hook lint (a mechanical gate on prose is brittle where a sentence is the honest answer); measured by `usage.md`'s existing `out=` per stage, archived runs before the sync against runs after. Words only here (stub 1, `terse-sessions/output-doctrine`); wiring the ten stop steps and the four skills to cite it, and `handoff.md`'s Blockers line naming the operator act, is stub 2 (`wire-stop-reports`); the before/after measurement is stub 3. Rejected: trimming further to fit a hard line cap (the acceptance bar is the stop-report shape and the never-trimmed list stated in full, not a byte count); a Stop-hook that lints prose length (brittle, and enforcement by hook is exactly the kind of mechanical gate the ethos avoids for judgment calls). Source: Jamie, 2026-09-24 — "prompt the agent to be as little verbose as possible while maintaining a clear path for what is being done … the files generated at the various stages should give enough of a history as well as the PR body itself … operator actions left to do can be formatted to always be a checklist within the chat"; two rounds of questions the same session, seven answers, recorded in `.icm/intake/terse-sessions/breakdown.md` **Revised 2026-09-24 (stub 2, before any repo synced it):** the first wording read as "say almost nothing" — gates came out as untickable `- [ ]` boxes in chat and the useful detail (the stubs in order, what was parked, what the spec chose) was dropped as "recap". Jamie: "we don't want no information, we want valuable information in as restrained a format as possible. The PR body and the markdown files can be as verbose as needed, I just want the chat to be easier to parse." So: the doctrine governs the chat only and never shortens a file; gate checkboxes live in the PR body alone, and chat names the gate with its PR link; the stop shape gains 2–5 bullets of what matters to steer (decisions, surprises, what was parked); `Operator:` is a numbered list, not boxes; a mid-session line is also allowed for a notable event, not only a phase change. | 2026-09-24 | — |
| D41 | **The UAT database is a second Marketplace database, connected to the UAT environment and Preview; production's database is connected to Production only.** D39 (3) made it a named branch of production's Neon project with its strings set by hand on the custom environment, and the integration "not connected to" it. On berceo's cutover (2026-09-24) that sent every UAT build to **production**: a custom environment is preview-type, and the production database — connected to Preview with preview branching — ran its Preview Deployment Action on each UAT deployment, overrode the environment's variables and bound it to Neon `main` (its URLs appeared on production's Auth trusted origins; `0001_comptes` reached production ahead of the batch's promotion). Ticking the custom environment on that database instead handed UAT the database's Preview secret — production's string again. Vercel documents both (*Storage on Vercel Marketplace → Use a Marketplace resource in a Custom Environment*, updated 2026-09-17: the Preview Deployment Action fallback "if that resource has no connection for the deployment's Custom Environment"; "For a Custom Environment, Vercel first uses the provider's Preview secret value"). So: (1) **two Marketplace databases per UAT repo** — production's (its Neon project, connected to **Production only**, preview branching off) and `uat-<repo>` (a separate Neon project, connected to the **custom environment + Preview** (+ Development), preview branching on). UAT reads the second's default branch; PR previews get `preview/<branch>` inside it; nothing non-production is ever wired to production's project. (2) **Every database variable is the integration's** — no hand-set strings, no `UAT_*` names, no code change (Jamie: "easily maintained and not a headache"). (3) **Non-production data never starts from production's**: the UAT database is built by the migrations the UAT build already runs, and previews and runs branch from it — a GDPR gain (no personal data copied into previews). The cost: `db-env.sh reset-uat` has no parent to reset from (re-migrate and re-seed instead), and the template's one `database.neon.project_id` splits into production's and the non-production project's (`one-branch-two-targets/uat-database-resource`). (4) Neon Auth, where the app uses it, is configured once on the second database to match production's. Rejected: app-level `UAT_DATABASE_URL`-style names read under `VERCEL_TARGET_ENV` (works against any integration, but a second set of variables per repo and a code change in each — Jamie refused it); the production database also connected to the custom environment (its Preview secret is production's string); preview branching turned off on production's database with previews on nothing (preview builds lose their database). Proven on berceo 2026-09-24: UAT build `dpl_Bj7r…` migrated `uat-berceo`; probe k0d0minio/berceo#28 got its preview branch there; production untouched. MongoDB repos keep D35–D37 (no Marketplace override path) | 2026-09-24 | D39 clause (3) (the UAT database as a named branch of production's project, `database.neon.uat_branch`) |
| D42 | **A migration declares itself irreversible in the file, not in `project.json` or a throw message.** `db-branch.sh prove`'s static check passes a `down` that throws — sustentus's convention for a manual backfill or a destructive rename with no honest revert — which then gives a real `UNPROVEN` when the round trip actually calls it (`triage/db-branch-prove-declared-irreversible`, D35's follow-up). Asked Jamie how a repo should mark one: an in-file marker the proof can grep without executing anything, a `.icm/project.json` list, or a throw-message convention matched in the runner's output. Jamie chose the in-file marker. Syntax: `irreversible = true` in the migration file itself (an exported const, or the CommonJS `exports.irreversible = true`) — read by grep, one home next to the migration it describes, and never requires running the down it excuses. `db-branch.sh prove`: the file needs no `down` export; the round trip's down stops at the NEWEST declared-irreversible own migration (not the oldest — a down assumes an unbroken chain back from the current state, so any own migration at or older than that one is excluded from down-testing too, not just the one that throws); everything is still proven up and idempotent (the forget-and-reapply check runs across all own migrations unchanged); the excluded migration(s) are named in the output as "declared irreversible", never silently passed. Rejected: a `.icm/project.json` list (a second place that drifts from the migrations themselves — one home per fact); a throw-message convention (requires running the down to see the throw, which defeats the point of skipping it). Proven on a fixture runner (file-backed fake `mongodb` driver + a `Migrator.run`-shaped stand-in, five scenarios): a declared-irreversible migration mid-branch is skipped and named, only the newer ones are down-tested; the same throwing `down` left undeclared still gives `UNPROVEN` (regression held); every own migration declared irreversible degrades to a forward-only-shaped proof (down never exercised); the baseline full round trip is unaffected; a migration with no `down` and no marker still fails the static check. Source: Jamie, 2026-09-24 | 2026-09-24 | — |

## Open questions

- Does the estate roster come from the `k0d0minio` org listing, or from Neon
  (`biz.clients.github_repo`) the way the dashboard's board does? The workflow currently
  assumes the org. *Answerable by: Jamie. Blocks: nothing yet — it only affects which
  repos get checked.*
- Should `icm-board` hold the estate `.icm/docs/` research (contabilista, founder brief)
  that stayed in `jamienisbet`? *Answerable by: Jamie. Blocks: nothing.*

## Run log
| Date | Commit | What changed |
|---|---|---|
| 2026-08-26 | — | Seeded at the split. Not a `/project` run; intent taken verbatim from the session that created this repo. |
| 2026-08-26 | — | The second-brain build (cloud session, interrogation-driven — decisions D3–D9 are Jamie's answers verbatim). Three workspaces created, rituals rehoused, knowledge layer scaffolded, canonical asset library seeded into the template. ICM-006 amended; ICM-009/ICM-010 cut. |
| 2026-08-26 | — | ICM-009 done in the same session: questionnaire run conversationally, all seven sections. Knowledge layer + target-profile + outreach filled with Jamie's real answers (band €1.000–€2.500 sites, €500 floor, method-not-band for apps/AI, scoped retainers €200–€4.000/mo, 50/50 default, two revision rounds, both ownership patterns, stack confirmed as policy, profile weighted to AI). Deliberate remaining gaps: voice/outreach example pastes, app/AI standing inclusions, the AI-SME channel. |
| 2026-08-28 | — | Estate ticket audit (Jamie-directed, 27–28 Aug): every open ticket checked against its codebase; board 85 → 54 open. Here: ICM-002 folded into ICM-001 (row above updated on Jamie's instruction), ICM-004 trimmed to its two live items. New: AGORA-020 (tracked credential PDFs) and REMI-038 (patient Stripe subscriptions) cut on his answers. Not a `/project` run — a directed register correction. |
| 2026-08-28 | — | **The pipeline rework** (Jamie-directed; four-agent analysis + two interrogation rounds — decisions D10–D13 are his answers). TICKETS.md rewritten, PIPELINE.md cut, the template rebuilt with the `pipeline` profile, all five scripts reworked, `/day`/`/project`/`/icm-check` contracts redesigned, own intake migrated (9 flat tickets → 4 epics + 1 triage stub; rollout cut as the estate-migration epic). Design record: `.icm/docs/pipeline-rework-design.md`. Dashboard counterpart in jamienisbet's own PR. |
| 2026-08-28 | — | **The fresh-footing sweep** (Jamie-directed, same day): the board's last seven stubs addressed to zero — barzinho P&L PDFs untracked (scrub = AUDIT P1, his call) · D15 decided and shipped (conformance gaps exit 0) · the discovery bank distilled from berceo + messy-play (22 → 44 questions) · business-state epic and the heartbeat dropped on his rulings. All four epics banked to `intake/_done/`; the estate board is empty, sustentus its own project. |
| 2026-08-28 | — | **The D14 clean slate** (Jamie's call, same day): ~39 open + ~130 archived legacy tickets purged across the estate; this repo's recut archives removed from the rework branch and every archived-original reference swept; the estate-migration epic re-cut to the single seeding stub. `JN-036` left on jamienisbet `main` deliberately — PR #78 moves that file, so it is purged after that merge. |
| 2026-09-18 | — | **The v5 pipeline refactor** (Jamie-directed: `ICM_Refactoring_Directive_v5.md`, grounded in the dry-run gap analysis and the deep-dive stage audit — decisions D20–D21). The pipeline template re-founded from sustentus's current `.icm/` (four stages, four lanes, `intake/CONTEXT.md`, five shared docs, twelve factory scripts + `lib/`), generalised by removing its identity; the `approve` substage deleted from the template, `icm-check.sh`, `PIPELINE.md` and the seeded router; file-level ownership recorded in `template/icm-pipeline/MANIFEST`; `icm-sync.sh` written and `icm-check.sh` rewired to the manifest (drift report + `--repo`); `env-check.sh`, `validate-decisions.sh`, `lib/project.sh` and the `P` stubs added; sustentus given `project.json`, `project-rules.md`, `notify.sh`, the profile line, and synced as the first target (its own PR). Verification: `.icm/docs/2026-09-18-icm-refactoring-verification.md`. Downstream rollout cut as `intake/pipeline-file-ownership/downstream-rollout`. |
| 2026-09-18 | — | **Downstream rollout — remi-ai** (Jamie-directed; the third stub of `pipeline-file-ownership`, which completes the epic — no other repo declares the pipeline profile). On remi-ai's branch `claude/icm-pipeline-sync` (k0d0minio/remi-ai#105, Jamie's to merge): project-owned files written first (`project.json`, `project-rules.md`, a validator-clean `knowledge-map.md`, `notify.sh` wired to Resend with a plain `SKIPPED` where the secrets are absent, `format.sh`/`lint.sh` on the repo's own tools), `icm-sync.sh --apply` (28 files; 0 differ afterwards; second apply `UNCHANGED`), `approve/` · `send-ship-note.sh` · `_design/` removed, three merged runs closed out with the synced `close-out.sh` (`nutrition-knowledge` archived as a completed epic), the labels workflow on the `origin/$BASE_REF...$HEAD_SHA` spelling, the gates workflow renamed `(advisory)`, `.prettierignore` guarding the template-owned paths, and the backlog retriaged into the template's stub shape (the fifteen decisions of record re-homed as D-1 … D-15 in `intake/README.md`; every batch `validate-intake.sh` OK). `icm-check.sh --repo`: no gaps, no pipeline drift. Found on the way and fixed in the template: `validate-spec.sh` hardcoded sustentus's persona vocabulary — it now reads `personas` from `.icm/project.json` like `project-labels.sh`; sustentus drifts on that one file until its next `--apply`. |
| 2026-09-18 | — | **`/project` absorbs the pipeline setup** (Jamie's request, after the remi-ai rollout): `deliver/project` gains § 1c — declare (Jamie's answer, never the fix's), guard the formatter before any template-owned file is committed, `icm-check.sh --fix` then `icm-sync.sh --apply`, read the repo for what the project-owned files can take from it — with § 3 asking the rest in the intent rounds, § 6 writing `project.json`, `project-rules.md`, `knowledge-map.md`, the hooks and the repo's own surfaces before the register, and the Audit proving it (`env-check.sh` PASS, second apply `UNCHANGED`, `icm-check.sh --repo` clean, validators OK). `PIPELINE.md` § profiles and the conformance gate point at it. Triage stub `project-onboards-pipeline-profile`, done in the same PR. |
| 2026-09-21 | — | **Template refinement — one pipeline, four additions** (Jamie-directed: *Central ICM Template Incremental Refinement & Sync Verification* — decision D22). The profile gate removed from `icm-sync.sh`, `icm-check.sh` and `ticket-hygiene.sh` (which now asks whether the repo carries the `/pipeline` router); `project.json` gains `complexity`; `select-model.sh`, `check-migrations.sh` and `process-raw.sh` added as template-owned scripts with `raw/` + `processed/` scaffolding; run-scoped isolation stated in the preamble and every stage and lane; Release step 7 checks the migration order; Scope step 2 records a processed source and retires its pointer stub. Verified on sustentus only (dry-run → apply → apply again, `UNCHANGED`); no other repo touched. The wording sweep of the contracts that still describe two profiles is parked in `triage/profile-wording-sweep.md`. |
| 2026-09-22 | — | **The deal-workspace rework** (Jamie-directed: the master directive, saved as `.icm/docs/2026-09-22-rework-master-directive.md` — decisions D23–D26; the design brief `2026-09-22-icm-board-rework-brief.md` it names was **absent** from both checkouts, so the session proceeded on the directive alone, as it instructs). Two PRs, nothing merged: **k0d0minio/icm-board#46** (`783e9e7` A agency layer · `1838faf` B knowledge and contracts · `b7d8123` C the deal workspace · `3d71e11` D template additions · F this row) and **k0d0minio/jamienisbet#133** (seven commits E1–E7: `deal_slug` + `support_minor` with migration 0024, the read-only deal reader and stage chip, forms read from icm-board and the answers snapshot into the deal folder, `/start`, the pick-up verb, positioning copy, the €500 floor on the referral site). Proofs recorded in `.icm/docs/2026-09-22-rework-verification.md`. **SKIPs:** pandoc absent → `render-deal.sh` `RESULT: SKIP` on every engagement (DOCX rendering unproven); `whisper-cli` absent (ffmpeg present) → `process-raw.sh` reports the audio kind `skipped … missing: whisper-cli` and was proven through a shim; no Vercel token in the session → `deploy-status.sh` / `rollback.sh --vercel` proven only to `SKIP`; no `opencode run` was launched and the `icm-session-env.js` shell.env plugin is not installed on this machine (the OpenCode reader was proven against the SQLite store with a real session id); the design brief absent. Verified on sustentus by dry-run only: `icm-sync.sh --dry-run projects/sustentus` → `RESULT: DRY-RUN 29`, twice identical; nothing applied anywhere; `git -C projects/sustentus status` and `remi-ai` unchanged. The profile-wording sweep (`triage/profile-wording-sweep.md`) done and moved to `_done/`. |
| 2026-09-23 | — | **The retrospective collector** (Jamie-directed: *Continuous Learning Engine & Retrospective Collector* — decision D27). `retrospective.sh` added to the template as a `T` script (the signature ladder, archive counts, `--min`, `--apply` into `## Learned rules`); `error.log` given its shape in Build's Outputs and its writing moment in Build step 4 / step 9 and every lane's RED path; Release step 7(b) and each lane's finish run it before the close-out and their records carry `- learned:`; Build and the lanes read `project-rules.md` → Learned rules as an Input; the `project-rules.md` and `runs/README.md` stubs, `MANIFEST`, `PIPELINE.md`, `template/README.md` and the seeded router updated. Proven on a fixture repo only (`.icm/docs/2026-09-23-retrospective-verification.md`): every verdict exercised; no estate repo touched, nothing synced — sustentus and remi-ai pick it up at their next `icm-sync.sh --apply`. |
| 2026-09-23 | — | **The deal-folder sweep** (Jamie-directed; decision D28). Every `projects/` repo inventoried against `workspaces/deals/`; 13 repos need no folder (personal, archived, or someone else's); five folders removed as a one-off and five renamed to the repo's name, each its own `Deal:` commit on `main`; the slug rule re-pointed in `deals/README.md`, `01_intake`, `07_kickoff`, the kickoff checklist, `start/CONTEXT.md`, CLIENTS.md and `render-deal.sh`'s example. New folders and the in-delivery engagement folders follow one relationship at a time. Follow-up in `jamienisbet`: the `deal_slug` migration and the dashboard's folder lookup by `github_repo`. |
| 2026-09-23 | — | **System enhancements from the directive** (Jamie-directed; D29): `.icm/skills/` (three seeded three-tier skills + `list-skills.sh`), `select-model.sh --stage/--complexity` (tiers + advisor/executor/validator), `db-branch.sh` + millisecond migration stamps in `check-migrations.sh` (`--new`, `MISNAMED`), `security-check.sh` (gitleaks + fallback + audit, redacted `error.log`), `run-pack.sh` + `_shared/run-pack/` (the seven canonical run files; `new-run.sh` seeds, `close-out.sh` syncs learned rules). Stage and lane contracts, the preamble, the router, `env-check.sh`, `setup.sh`, `lib/project.sh`, the MANIFEST (+15 T), `project.json` (`migrations.stamp/tool/out_of_order`, `database`, `security`) and the canonical `session-start.sh` updated. Verified in a scratch repo built from the template with a stubbed GitHub API: `.icm/docs/2026-09-23-icm-system-enhancements-verification.md`. Not done: the estate sync (`icm-sync.sh --apply` per repo) — its own session. |
| 2026-09-23 | — | **The health probe** (Jamie-directed: *Agent Directive — Security Audit Gate & Post-Deploy Health Verification*, "Prompt 3" — decision D30; k0d0minio/icm-board#49). `health-check.sh` added to the template as a `T` script (one GET per `health_endpoint`, 200, three attempts with backoff; a failure → `report.sh alert` + one uncommitted `lane: bug` · `complexity: high` triage stub per merge SHA); `lib/project.sh` reads `health_endpoint`; `env-check.sh` step 9 reports the audit tool and a declared endpoint; `/setup` asks for the endpoint and `setup.sh` warns until it is set (Jamie's ask, same day); Release step 9(a) and the hotfix lane read it; the intake contract names the second script that parks a stub. **Proven in a scratch repo:** the probe against a local server (SKIP · OK · DRY-RUN · FAIL with the alert `SKIPPED` on the seeded default and on an unset Slack token, the stub written once and lane-valid, `000` on a refused port, `--header-env`), `env-check.sh` PASS with step 9, `setup.sh --report`'s health line as `[WARN]` with a deploy block and `[OK]` once set, `icm-sync.sh --dry-run` listing the new file. **The branch's own `security-check.sh`** (added-lines scan, live `npm audit` proven on a planted vulnerable lockfile, error.log entries read by `retrospective.sh`) was **withdrawn at the merge in favour of D29's**, which landed first on `main`; Release step 4 keeps the audit on every read and the operator's waiver (Jamie reversed the first cut's dependency-change gating on review). Also merged in: D27, D28 and D29 as `main` moved three times under the branch; the eight dead links today's deal-folder renames left in `.icm/docs/2026-08-26-icm-011-adoption-sweep.md` were fixed on both sides identically (`fix-dead-deal-links-adoption-sweep` done). **Unproven:** the gitleaks route (not installed here), a real alert channel. Rollout parked as `triage/sync-security-gate-and-health-probe`. |
| 2026-09-23 | — | **UAT batch + the client status report** (Jamie-directed: the *UAT Batch Preview Environment & Async Client Status Generator* directive, saved as `.icm/docs/2026-09-23-uat-client-status-directive.md`, with his framing — optional and only through `/setup`, one consistent environment rather than one per batch, the report everywhere — decision D31; D30 is #49's, the health probe). Template: `uat/CONTEXT.md`, `scripts/client-status.sh`, `scripts/promote-uat.sh`, `output/.gitkeep` (four T lines in the MANIFEST); `lib/project.sh` gains the `uat` keys, `pipeline_base_branch` and the `promote` lane; `project.json` the empty `uat` block; `new-run.sh` (base branch, the main bring-in), `close-out.sh` (the batch append), `deploy-status.sh --uat`, `check-migrations.sh` (base), `setup.sh` § 3/10 read the block; Define/Build/Release, the five lanes, `github.md` (regime 4), `ci.md`, `project-rules.md`, the router (`status`, `uat …`) and `/setup` skills, and the reference release workflow (default-branch merges only) carry the rule. Smoke-tested on a fixture repo, not on an estate repo; estate rollout not run — `icm-sync.sh --apply` per repo is Jamie's, and `/setup` per repo decides whether UAT is declared. |
| 2026-09-23 | — | **The template-change guard** (Jamie's ask, the template having run smoothly on three repos — decision D33). New `T` file `_shared/template-change.md`: the check, the procedure, the override, the `## Prompt` shape. One reference each in the MANIFEST header, the stage preamble (isolation rule 6), the router (step 9, the lane-argument resolver, the help, the extension note), `/setup` step 2, the bug/tweak/chore lanes' first step, the intake contract (a fourth writer of pointer stubs) and the `.icm/CONTEXT.md` baseline; on this side PIPELINE.md (§ File-level ownership, the tree, § Adding a stage or lane), the template README, the house doctrine, the Layer-1 routing table and the conformance stage. Synced the same session into berceo (k0d0minio/berceo#18), agorasim (k0d0minio/agorasim#115) and remi-ai (k0d0minio/remi-ai#117) **from this branch** — all four PRs green on first push (their `.icm/template-version` stamps the branch commit; a re-run of `icm-sync.sh --apply` from `main` after the merge reports UNCHANGED and re-stamps) — the router and `/setup` skills copied by hand (no local edits in any of the three; their diffs were the template moving under them at #53/#54), the travelling rule added to each repo's own `.icm/CONTEXT.md`, one standing rule in each `AGENTS.md`; through PRs, because berceo and remi-ai require one on `main`. Unproven: a live session meeting a template-owned edit request and producing the stub — the first real case is the proof. |
| 2026-09-23 | — | **Sustentus synced to the template, and the template taught the MongoDB migration form** (Jamie's ask; decision D34). icm-board side: `check-migrations.sh` + `lib/project.sh` read the epoch form (`migrations.stamp: epoch`, `migrations.extension`, `tool: mongodb`), fixture-proven (STALE → RESTAMPED, MISNAMED → RENAMED, `--new` kebab-case `.ts`, the millis form unchanged); `env-check.sh`, the setup skill, PIPELINE.md, the template README, the project-rules stub and the database-migration skill say so; the four lines that said the tooling never touches sustentus (`_system/README.md`, `deliver/CONTEXT.md`, the conformance stage, AGENTS.md) now say what D20 says — exempt from the walk, synced like any repo on Jamie's word. Sustentus side (its own PR on `claude/icm-template-sync`, Jamie's to merge): the three project-owned files written first (`project.json` on the D23–D33 shape — `Quality Project`, the smoke check, the six personas, the docs-site archives, the eight Vercel projects, Slack for announce and alert with `announce_from: ci`, the epoch migrations, MongoDB with no isolation; `project-rules.md` re-homed from the closed #1138 and extended; `report.sh` seeded), then `icm-sync.sh --apply` (71 files; second apply UNCHANGED), the canonical router and `/setup` skill, `release.yaml` rewired through `report.sh` with its verify half kept, `.prettierignore` covering every T path, the three lane labels, the explicit allowlist. Not done here: Jamie's merges; `/setup` for the Vercel token name and the health endpoint; creating the three labels on GitHub; installing gitleaks. The closed #1138 branch is deleted after the new PR merges. |
| 2026-09-23 | — | **MongoDB databases per run and per preview, and the migration round trip** (Jamie's ask, sustentus the first adopter — decision D35). Template: `scripts/lib/db-name.mjs` and `scripts/lib/mongo.mjs` (two new T lines — run with node, never executable); `db-branch.sh` gains `isolation: database` and the `prove` verb; `db-env.sh` a MongoDB section (`status`, `init`, `prune`, `reset-uat` on a UAT repo); `lib/project.sh` the `database.mongodb` readers and `provider: mongodb` (url_env defaults to MONGODB_URI there); `project.json` the empty block; `setup.sh` § 3 (no warning on `isolation: database`; names, commands and the cluster read once) and § 10 (`mongodb-cleanup.yaml`); `env-check.sh`; Build step 10 and Release step 7 + stop class 3 + Verify; `uat/CONTEXT.md`; the `database-migration` skill (step 5, the MongoDB row's round-trip rule, `preflight.sh` proves on a database repo); `project-rules.md`'s two database lines; the `/setup` questions; the reference `github-pipeline/workflows/mongodb-cleanup.yaml`; PIPELINE.md and the template README. **Proven on a fixture repo with a file-backed stand-in for the driver** (no mongod on this machine): the four names and `databaseName()`'s three cases; `up`/`env`/`status`/`down` and the pointer line; `prove` → PROVEN on a good migration and UNPROVEN (exit 2) for a down that leaves an index, a missing down, an up that throws when re-applied and an up that changes shape when re-applied; forward-only → PROVEN; no own migrations → SKIP; the guards (production and shared preview refused by name even when `preview_*`-shaped, the UAT database outside `--uat`, `forget` outside `run_*`); the no-`dropDatabase` fallback; `db-env.sh status`/`prune`/`prune --apply`/`reset-uat`/`init`. **Unproven:** a real cluster and ts-migrate-mongoose (the fixture runner copies its shape — `up [name]`, `down <name>`, autosync, prune); the cleanup workflow on GitHub; the app-side derivation on a Vercel preview. Rollout parked as `triage/sync-mongodb-databases-sustentus`. |
| 2026-09-23 | — | **D35 rolled into sustentus — steps 1, 2 and 4; the live `prove` not yet run** (`triage/sync-mongodb-databases-sustentus`). sustentus/sustentus#1143 (`claude/sync-template-d35`, unmerged): `icm-sync.sh --apply` → 18 T files, stamp `6a8537d`; `/setup` → `provider: mongodb`, `isolation: database`, `previews: branch`, production `sustentus-prod`, shared preview `sustentus-preview` (read from the GitHub environments' variables), `limits` left at the shared-tier defaults because the Atlas connector sees none of sustentus's clusters, `mongodb-cleanup.yaml` seeded with `environment: preview`; `setup.sh --report` OK. The chore: `connection.ts` reads `databaseName()` from a **verbatim copy** in `packages/services/src/db/` (the package's `rootDir` refuses the `.icm/` path; a `.d.mts` types it, a vitest test fails on drift, ESLint ignores it); `db-migrate.yaml` migrates `preview_<branch>` on a per-branch queue and `preview-smoke.yaml` waits for it — both **gated on a repository variable of the flag's name**, because a workflow cannot read Vercel's Preview target: the switch has two halves in sustentus, set together and unset together (a departure from D35's "one flag", recorded in its `project-rules.md`). The trigger's `paths` filter moved into the job — a per-branch preview needs its database whatever the PR touches. **Step 3 not run:** `MONGODB_URI` is not in the session's environment, and Jamie chose to defer it. **What the fixture runner got wrong about ts-migrate-mongoose (read from `ts-migrate-mongoose@4.2.2` source and sustentus's 92 migrations, not yet measured):** (1) `up <name>` applies every pending migration with `createdAt` ≤ that one's, and `down <name>` reverts every applied one with `createdAt` ≥ it — the fixture reads these as "up to" and "this and after" by file order, which only holds when the branch's own stamps are newer than the base's; with `out_of_order: true` (sustentus's setting) an own migration stamped older than main's newest lands in the "base shape", and `down <first>` then reverts base migrations too. (2) `db-branch.sh` seeds **before** it migrates, but sustentus migrates first and seeds second; its seed compiles Mongoose models, which build the head's indexes (autoIndex, asynchronously) on the empty database. So the "base shape" can already hold the indexes a branch's migration creates, and the snapshot races the builds. That can give a false UNPROVEN on a correct `down`. (3) The static `down`-export check passes a `down` that throws: 6 of sustentus's 92 migrations throw "irreversible" and 5 more are deliberate no-ops, so `reversible: true` is false for the history, and the full-history round trip (`--base` at the first migration's parent) is UNPROVEN before it starts. A no-op `down` on a data-only migration still reads PROVEN: the proof compares shape (collections and indexes), never data. (4) `1786492800000-flag-demo-tenant` fails loudly when `DEMO_TENANT_CLERK_ORG_ID` is set but matches no tenant, which is the case on a fresh `run_*`, so the live `prove` must run with that variable unset. (5) The stub's "93 migrations" is 92 plus `template.ts`. Template follow-ups for (1) and (2) are parked with the stub. |
| 2026-09-23 | — | **D36: non-production on its own cluster; per-branch previews copied from the shared preview database** (sustentus follow-up to D35; sustentus/sustentus#1143 merged `a51f3b9a0`, #1146 open). While working the operator checklist, Jamie found three things. (1) sustentus's one `Vercel-Admin-sustentus` user, created by the Vercel MongoDB Atlas integration, served production and every preview, so a preview credential could write production; its password was also exposed in a pasted command, and the checklist retires it. (2) The seed creates no tenant; the demo tenant was built by hand from a runbook (sign-in, flag migration, `demo:roster`, "Populate demo data"), so a seeded `preview_<branch>` opens on nothing, and `flag-demo-tenant` fails without a tenant. (3) The Vercel "expose system variables" toggle no longer exists. Jamie created a second free M0 (`sustentus-staging`, its own Atlas project) and chose mongodump/mongorestore. Template: `db-env.sh init` drops the toggle, recommends the split cluster and the by-hand URIs, names the copy for a repo whose seed makes no tenant, and says the repository variable goes beside the Preview flag; `status` reads a missing production as expected; `project-rules.md` loses the "system variables exposed" slot. sustentus #1146: `db-migrate.yaml` copies `sustentus-preview` into `preview_<branch>` on first push, after a caps check, and migrates `sustentus-preview` on each merge when the switch is on; `project-rules.md` records the two clusters and where each secret points. **Unproven:** the copy step on a real runner; the operator cutover (phases 1–7 of the session checklist). Still true: about 60 collections per database, so about 6 previews/runs fit on M0 beside `sustentus-preview`. **Then Jamie turned off run databases** (`database.isolation: none`, in #1146): sustentus runs never migrated locally, its migrations are forward-only in practice, and CI migrates each PR's migrations against a real database before merge, so `prove` now reports SKIP there. `setup.sh` warns on `isolation: none` whatever `project-rules.md` records; that warning is accepted as a named decision. Per-branch previews are built but left off until concurrent schema PRs hurt. |
| 2026-09-23 | — | **D36's first real PR fails the copy step: Atlas's shared-tier 38-byte database-name cap, not MongoDB's 63** (`triage/sync-mongodb-databases-sustentus`, blocked on new `triage/mongo-preview-db-name-atlas-shared-tier-cap`). sustentus/sustentus#1150 (`claude/sentry-web-instrumentation`) was the first ready PR after Jamie's cutover (`sustentus-preview` restored on `sustentus-staging`, both URIs split, `MONGODB_PREVIEW_PER_BRANCH=1` set on GitHub and the three Vercel projects). Its `Migrate preview database` step ran the mongodump/mongorestore copy and Atlas refused it: `preview_claude_sentry_web_instrumentation` is 42 bytes, under `lib/db-name.mjs`'s `MAX_DB_NAME = 63`, but `sustentus-staging` — a shared/free (M0) cluster, same as every adopter so far — caps database names at 38 bytes, a stricter limit than the dedicated-tier one the template assumed. mongorestore's failure then breaks the pipe mongodump was writing into, which read as a second error but is the same root cause. Parked as `triage/mongo-preview-db-name-atlas-shared-tier-cap` (P1): make the cap configurable via `database.mongodb.limits` (default 38) instead of the module constant. The stub stays open until the fix is synced and a PR runs the copy, the preview migrate, the smoke wait and cleanup clean. |
| 2026-09-23 | — | **The database-name cap follows the cluster — decision D37** (`triage/mongo-preview-db-name-atlas-shared-tier-cap`). Template: `lib/db-name.mjs` (`DEFAULT_NAME_BYTES = 38` replaces `MAX_DB_NAME = 63`; the cap a parameter everywhere; the CLI reads `limits.name_bytes` from `.icm/project.json`), `lib/mongo.mjs` (the UAT name), `lib/project.sh` (`mongo_limit_name_bytes`), `db-env.sh` (`init` shows the cap and, off 38, the connection line's third argument), `setup.sh --report`, `project.json` (P — the default made visible), the `/setup` question. **Proven on a fixture repo:** `claude/sentry-web-instrumentation` → `preview_claude_sentry_web_ins_65692577` (38 bytes) with no `name_bytes` and with no `project.json`, from another cwd and via `--stdin`; `name_bytes: 63` → the 41-byte name unchanged; 64, 19, 37.5 and a broken `project.json` exit 1 with the reason; `databaseName()` on import agrees with the CLI and reads no file. (The stub's "42 bytes" is 41.) **Unproven here:** Atlas accepting the name — the sync into sustentus and #1150's re-run prove that. |
| 2026-09-23 | — | **`db-branch.sh prove` follows ts-migrate-mongoose's stamp order, and seeds after it migrates** (`triage/db-branch-prove-runner-semantics`, D35's follow-up). Read from `ts-migrate-mongoose@4.2.2` `Migrator.run`: `up <name>` applies every pending migration stamped at or before it, `down <name>` reverts every applied one stamped at or after it, and `--single` runs exactly the one named. Template: (1) **migrate first, seed second**, everywhere the template runs both: `db-branch.sh up`, `db-env.sh reset-uat`, and the wording in the skill, PIPELINE.md, project-rules, uat/CONTEXT, setup and the cleanup workflow. This is sustentus's own order, and a seed written against the head's models belongs on the migrated shape. `prove` no longer seeds before the proof at all: the base shape is an unseeded database migrated through exactly the base's migrations, and the seed runs only after `PROVEN`. So the head's autoIndex builds can no longer put a branch's index into the "base shape". (2) **Out of stamp order** (an own migration stamped before one of the base's, which `out_of_order: true` allows): the base's migrations before the branch's oldest stamp go up in one `up <name>`, and those at or after it go up one at a time (`up <name> --single`). The round trip then reverts only the branch's own migrations, newest first (`down <name> --single`), never a base one. In stamp order nothing changes, and `--single` is never passed. The migrate-command contract gains `[--single]` (db-branch.sh header, setup.sh, db-env.sh init, the /setup skill). **Proven on a fixture** whose runner ports `Migrator.sync` + `Migrator.run` line for line onto a file-backed stand-in driver (24 checks, main's script run beside the new one). The runner itself: `up <base newest>` takes an older own migration with it; `down <own>` hits the newer base migration first; `--single` runs exactly one. In order, with a seed that builds the branch's index: main gives a false UNPROVEN, the new script gives PROVEN and seeds last. Out of order, over a newer base migration whose `down` throws: main gives a false UNPROVEN, the new script gives PROVEN and never runs a base `down`. Still caught: two own migrations reverted newest first, one leaving its index (UNPROVEN 1); an own `down` that throws; a non-idempotent `up`. Also checked: forward-only gives PROVEN, no own migrations gives SKIP, `up` and `reset-uat --apply` migrate before they seed, and the URI is redacted. **Unproven:** a real cluster and the real ts-migrate-mongoose binary (no mongod on this machine), and real autoIndex timing. sustentus now runs `isolation: none`, so no live repo exercises `prove`. **Parked:** a throwing `down` read as "declared irreversible" needs a decision on how a repo declares it: `triage/db-branch-prove-declared-irreversible`. |
| 2026-09-23 | — | **The template's scripts read and guard the ticket base branch — D38, stub 2** (`ticket-base-branch/template-ticket-scripts`). `client-status.sh`: "in progress" and "queued" read `.icm/intake/` on `origin/<pipeline_base_branch>` (delivered stays the archive on `main`; UAT unchanged), and the foot names the ref. `wrap-reminder.sh` (T, and icm-board's own copy, byte-identical): compares with `origin/<uat.branch>` read by `jq` from `project.json` (no `.icm/scripts/` needed), falls back to `main` when the branch is undeclared or not on origin, and takes the nearer of the two when a branch was cut from `main` on a UAT repo (hotfix, knowledge lane); its message says the board reads the ticket base branch. `release.yaml`: skips a PR labelled `type:tickets` (the body's `announce: none` stays the second guard). `setup.sh` checks `type:tickets` in `.github/labels.yml` on every repo; `labels.yaml`'s header names it. `promote-uat.sh`: `sync` is required after a hotfix *or* a knowledge-lane change; `init` asks the operator to keep the admin bypass on a checked UAT branch. `new-run.sh`: comments and the merge message no longer call `main` the home of the intake cut. `_shared/ci.md`: an optional cost recipe (`deploymentEnabled` off for `claude/tickets-*`, an `.icm/`-only ignore step). **No `ticket-pr.sh`** (Define's call): the procedure is one path-guard `git diff` and one `gh pr merge`, it already lives once in `pr-conventions`, most client repos have no `.icm/scripts/`, and a script that merges would be a second home for it. **The citations of #66 said D37; the register row is D38** (#64 took D37 while #66 was open) — every ticket-base citation now reads D38. **Proven on scratch fixtures** (bare origin, `uat` declared): a stub cut only on `origin/uat` is queued by the new `client-status.sh` and missed by the old; one closed on `uat` leaves the queue; with no UAT the output is byte-identical to the old script's. The hook: a ticket-admin branch cut from `origin/uat` is silent (the old one reported it); a work branch from `origin/uat` with its stub untouched is reported (the old one was silent), and silent once the stub moves to `_done/`; a branch from `main`, an undeclared or deleted UAT branch and a repo with no `project.json` compare with `main`. `release.yaml`'s job condition, evaluated from the file's text: a merged `type:tickets` PR → skip; run, promotion and dispatch → announce. **Unproven:** the workflow on GitHub itself (no runner locally); `new-run.sh`'s changed message (a merge path needing a GitHub route) — wording only. |
| 2026-09-23 | — | **D37 confirmed live; the last proof point for `triage/sync-mongodb-databases-sustentus` is `Preview smoke`, still unrun.** sustentus/sustentus#1150 merged (`b8b84bf5`): `Migrate preview database` and `Drop the PR's MongoDB databases` both passed, into and back out of `preview_claude_sentry_web_ins_65692577` — the name-cap fix (D37) holds against a real branch on the real cluster, and the copy → migrate → cleanup round trip that D36 promised now runs clean end to end. `Preview smoke` never fired: `Vercel – web`'s deployment for that commit (`dpl_ECvZgCVoAXQjqe3qZvRG5LvPr44b`) was still `BUILDING` when the PR closed, so no `deployment_status` success ever reached the workflow's trigger, and the preview database it would have checked is already dropped — unrelated to D35/D36/D37 or to sustentus's own `#418` hydration stub. The stub stays open for one thing: a PR whose web preview reaches `READY` before it closes, so `Preview smoke` actually runs against a `preview_<branch>` database. |
| 2026-09-23 | — | **icm-board's board and hygiene read each repo's ticket base branch — D38, stub 3** (`ticket-base-branch/board-scripts-ticket-base`). New sourced `_system/scripts/lib/ticket-base.sh`: the base is `uat.branch` from `origin/main:.icm/project.json`, else `main`; a client repo's `.icm/intake`, `runs`, `dormant` and `.claude/skills/pipeline/SKILL.md` are materialised from `origin/<base>` (`git archive`) into a scratch dir — never the shared checkout; no ref → the working tree, noted on stderr; icm-board reads its own disk. `tickets-board.sh` and `ticket-hygiene.sh` read through it, hygiene's `possibly-done`/`off-ticket` walk `git log origin/<base>` and `today-unresolved` resolves on the same view. Found in passing and fixed: the board keyed client repos as `projects/<repo>` while `today.md` names the bare repo, so no client pick ever grouped under Today. `/day` step 1 now runs `pull-all.sh` first and verifies at `origin/<base>`; steps 2 and 4 edit a client repo in a worktree off `origin/<base>`, the one step 5 ships from. CI's shellcheck covers `_system/scripts/lib/`. **Proven on a scratch fixture** (bare origin, `uat` declared, a stub `_done` on `uat` and open on `main`, a run merged only into `uat`, a local-only repo): the done stub is off the board and flagged by `today-unresolved`, `possibly-done` finds the `uat` merge, the checkout is left on `main`, clean. **Read-only against the real estate**: agorasim and berceo stubs finished on `uat` leave the board, stubs cut on `uat` appear (243 → 244 open, 358 → 362 done); hygiene unchanged (clean); `--today` 9.2s → 10.5s. **Not proven:** a real `/day wrap` ticket PR — that is stub 4's berceo proof. Decision-numbering review the same day: register D1–D38 contiguous; jamienisbet's epic cited D37 for this rule (fixed, k0d0minio/jamienisbet#148); every pipeline repo's `.icm/template-version` names a branch commit, not `main` (`triage/icm-sync-stamps-unmerged-commit`) |
| 2026-09-23 | — | **D38 rolled out to five of six pipeline repos; the estate template in sync** (`ticket-base-branch/ticket-base-rollout`, stub 4 — open for agorasim). berceo, jamienisbet, remi-ai, sustentus (Jamie's word, this session) and vinecliff synced to `de444cd` — every T file had matched its old stamp, so the drift was lag only — canonical `.claude/` assets refreshed by hand, `type:tickets` in each `labels.yml` and created in GitHub. berceo: `uat` took `main` by hand (the sync STOPped — #21 had carried its own `5bef762` sync onto `uat`), then berceo#24 proved the ticket PR end to end. Found: `neon-cleanup.yaml`'s `permissions: {}` broke checkout on a private repo, so agorasim's preview branches filled Neon's 10-branch cap and #121's preview could not provision — template fixed in #69; agorasim waits on Jamie's prune. Note: `.icm/docs/2026-09-23-ticket-base-rollout.md` |
| 2026-09-24 | — | **`sync-mongodb-databases-sustentus` closed — D35–D37 hold, and the one open item wasn't a database question.** With no further sustentus development to press it, re-read `Preview smoke`'s own record instead of waiting on one: it's a mechanism outside D35–D37 and it already works — `#1140`/`#1143` walked all six personas and correctly failed on a real bug (Web Analytics 404ing every sign-in page, fixed by `#1145`); `#1146`–`#1149` correctly skip when Vercel ignores an unaffected `web` build. `#1150` is the one attempt since that fix, and it never reached smoke because the `web` deployment itself errored (`dpl_ECvZgCVoAXQjqe3qZvRG5LvPr44b`, `BUILD_EXCEEDED_MAXIMUM_TIME`) — nothing to do with the database, and smoke's dependency on a working `preview_<branch>` connection was already exercised by the migrate step that passed on the same PR. The round trip D35–D37 promised — copy → migrate → cleanup, on the real `sustentus-staging` cluster, under the real Atlas name cap — is proven twice over (`#1150`'s two passes). The build timeout is parked on its own as `triage/sustentus-web-build-exceeds-vercel-timeout`, unrelated to this. |
| 2026-09-24 | — | **One branch, two targets — decision D39** (Jamie's ask: the UAT branch "leaves a lot of room for discrepancies … the tickets get tracked over 2 branches, the drift in branches is felt, … different configurations per project"; two rounds of questions, eight answers, recorded in the row). Investigated first: every template, contract and board file that reads the UAT branch (one switch, `pipeline_base_branch`; two direct re-derivations, `lib/ticket-base.sh` and `wrap-reminder.sh`; no Vercel promote call anywhere; a GitHub Release only ever an announcement); Vercel's documented mechanics (staged production deployments on all plans via the Auto-assign toggle, promote without rebuild, Staged/Promoted/Current; custom environments on Pro, one per project, `VERCEL_TARGET_ENV`, the Neon native integration scopable to one; a promoted *preview* rebuilds with production variables — so the production candidate is the staged build, never the UAT one); the live estate (kodominio and sustentus both Pro, custom-environment slot unused; agorasim `uat` 9 ahead of `main` and `main` 1 ahead of `uat` — the #126 knowledge merge `sync` never carried; berceo `uat` 4 ahead; both UAT domains bound to the git branch `uat`; `preview/uat` Neon branches on both; `db-migrate.yml` on `push: main` in both — the one production migrator, which under one branch would run before the client's word; berceo's two rulesets on `main`+`uat` with an admin bypass, agorasim none — GitHub Free, private). Register: D39 written, superseding D31, D32(2) and D38 as its row states; Layer 0 (`AGENTS.md`, `CONTEXT.md`, `_system/README.md`) reworded to the direct ticket commit; the `ticket-base-branch` epic archived — stub 4's remainder (agorasim's ticket-PR proof, the jamienisbet dashboard stub) dropped as superseded; the six-repo template sync it did stands. New epic `one-branch-two-targets`: `template-two-targets` → `board-reads-main` → `cutover-agorasim-berceo` (the ordered operator checklist — Auto-assign off *before* `uat` is merged into `main`, or the unsigned batch ships) → `estate-sync-d39`. Nothing in the template or any client repo changed yet; the whole route is unproven until berceo's first real promotion; serviflow set aside on Jamie's word. |
| 2026-09-24 | — | **icm-board reads and writes client tickets on `main` again — D39, stub 2** (`one-branch-two-targets/board-reads-main`). `_system/scripts/lib/ticket-base.sh`: the base is always `origin/main`, the `project.json` lookup (`ticket_base`) gone, the `git archive` materialisation kept — the shared `projects/` tree races are unrelated to UAT; `tickets-board.sh`, `ticket-hygiene.sh`: comments only. `wrap-reminder.sh` (both copies, byte-identical): D38's hunks reverted — compares with `origin/main` only; the message says push straight to `main`. `pr-conventions` (both copies): "The ticket PR — the one PR an agent merges" → "Ticket commits — straight to main, no PR": a worktree off `origin/main`, the same path guard (`.icm/intake/**`, plus `.icm/runs/<slug>/**` for Scope's front) checked before the push, `git push origin HEAD:main`, one `pull --rebase` retry, the operator's admin bypass where a ruleset guards `main`; `type:tickets`, `--admin` and the icm-board exemption gone; no PR is ever merged by an agent. `ticket-craft` (both copies) likewise. `/day` (stage + command description) and `/project` contracts: `origin/main` for the survey, a worktree off `origin/main`, one direct commit per changed repo; `/project`'s register still goes on its own `claude/` PR. `TICKETS.md`: the dashboard and local scripts read `main`; the three D38 working rules → one sentence pointing at D39 §8. Root `README.md` and `_system/README.md` reworded. Left for stub 1 on purpose: the template's `icm/CONTEXT.md`, `icm/intake/README.md`, `README.md` and `_system/contracts/PIPELINE.md` still describe the ticket PR — its breakdown names them. **Board before → after (same refs, agorasim and berceo fetched):** 250 → 243 open, 371 → 363 done, 9 repos, in flight / blocked unchanged; hygiene identical (1 finding). Every difference is agorasim or berceo and is exactly `origin/uat` vs `origin/main`: agorasim (`uat` 12 ahead, `main` 1 ahead) — 9 triage stubs born on `uat` vanish (fill-helper-consolidation, knowledge-map-web-docs-deleted, the five quote-* tweaks/chores, stripe-webhook-secret-development, superseded-quote-open-payments) and 4 finished there read open again (admin-quote-builder, quote-page-and-deposit-link, dependency-advisories-2026-09-23, experiencias-heading-and-meta); berceo (`uat` 5 ahead) — 6 born on `uat` vanish (the four comptes-* triage stubs, knowledge-map-site-as-built, uat-address-dns) and 4 read open again (comptes-neon-auth, socle-design-system, vitrine-publique, npm-audit-high-advisories). Stub 3's merge of `uat` into `main` closes the gap; until then the board is wrong on those two repos, so this lands with or just before the cutover, and no `/day` moves their stubs on `main` meanwhile (it would conflict with that merge). Unproven: `/day wrap` in a client repo ending in a direct commit to `main` — no client repo touched here; the first real wrap is the proof. |
| 2026-09-24 | — | **The template speaks one branch and two targets — D39, stub 1** (`one-branch-two-targets/template-two-targets`). Removed, clean cut: `uat/CONTEXT.md`, `scripts/promote-uat.sh`, every `batch.json` writer (`close-out.sh` 3b, `setup.sh`), `pipeline_base_branch`/`uat_branch`/the `promote` lane/the `preview/<uat.branch>` fallback in `lib/project.sh`, `new-run.sh`'s main bring-in; every caller reads `main`. New T files: `scripts/promote.sh {init,status,approve}` (approve DRAFTS a Release `release/<date>-promote-<sha7>` whose body carries `- promote-sha:`; never publishes, never promotes) and `_shared/promotion.md` (the contract). `project.json`: `uat: {target, url}` (both or neither), `database.mongodb.uat_name`; `neon.uat_branch` now required with UAT, never derived. `client-status.sh`: live = runs archived at the last release tag on `origin/main`, on UAT = since — output for a repo without UAT byte-identical to before (fixture). `deploy-status.sh --uat`: the custom environment's deployment (`customEnvironment.slug`, else its id, else `target` — which one v6 exposes is stub 3's proof). `setup.sh` fails `uat.branch`, a `batch.json`, a half declaration, a missing UAT database name, a non-Pro team (`accountLimit.total`), a UAT repo without `release.yaml` or with a push-only `db-migrate.yml`. `report.sh` (P) gains `--tag`, so the workflow reuses the published Release. Reference workflows: `release.yaml` rewritten (merge → announce only without UAT and with `announce_from: ci`; `release: published` → stage → migrate (calls `db-migrate.yml`) → promote → deploy-status → announce; concurrency `promote`), new `db-migrate.yml` (one reference, a gate job: push migrates only without UAT) and `uat-deploy.yaml` (only where branch tracking is refused); the cleanup workflows lose the UAT-branch skip. Words: stages 01–04, the six lanes, `_shared/{github,ci,stage-preamble,template-change,project-rules}`, `intake/CONTEXT`, the `pipeline` (`uat …` → `promote …`) and `setup` skills, `icm/`, the template README, `PIPELINE.md` (D39 once). `icm-sync.sh` reports the three retired paths. Proven on scratch fixtures with a local mock of the Releases API (no GitHub repo created, no Vercel touched): approve drafts and refuses a second draft, a SHA off `main`, a SHA already released; status computes the batch from the last tag and the floor moves on publish; the workflow's gate scripts run for nine event/body cases and the migrator gate for six. Unproven until berceo's first promotion: the Vercel reads and the promote call. Notes for stubs 3 and 4 written into them (`report.sh --tag` by hand; the baseline through `approve --announce none`). |
| 2026-09-24 | — | **The output doctrine — D40, stub 1** (`terse-sessions/output-doctrine`). New template-owned `_shared/output.md` (T in the MANIFEST): audience (chat for the operator, files for anyone reading later), one line per phase change while working, the one stop-report shape (`<stage/lane> <outcome> · CI <verdict> · <PR link>`, an `Operator:` checklist, `Unverified:` only when non-empty), the split between `handoff.md` and the `Operator:` checklist with the blocking-act exception, the never-trimmed list, and its scope and non-enforcement (the pipeline and the four canonical skills; not icm-board's own commands or the global CLAUDE.md; no output style, no Stop-hook lint). D40 recorded. No stage, lane or skill file touched — none of the ten stop steps cite it yet, and no `handoff.md` Blockers line names an operator act; both are stub 2 (`wire-stop-reports`). No client repo touched. |
| 2026-09-24 | — | **Every stop reports in one shape — D40, stub 2, with D40 loosened** (`terse-sessions/wire-stop-reports`, #74). The ten stop steps, `/pipeline` (step 1 loads the doctrine — every stage and lane routes through it), `/setup`, and `pr-conventions` / `ticket-craft` inline (both copies byte-identical) report per `_shared/output.md`; `handoff.md` Blockers names `blocked on operator: <act>`. Before merge Jamie read the first cut as too bare — gates in chat, detail dropped — so `output.md` was loosened in the same PR (chat only, gates stay in the PR body, 2–5 summary bullets, numbered `Operator:`). No client repo synced. |
| 2026-09-24 | — | **Baseline `out=` read from every archived run — D40, stub 3, half done** (`terse-sessions/measure-output`; `.icm/docs/2026-09-24-terse-sessions-measure.md`). Read-only, no script added: 17 archived runs across three repos (agorasim, jamienisbet, remi-ai), none synced to `_shared/output.md` yet, gave 30 stage/lane pairs — median `out=` 26,231 (build), 24,828 (chore), 13,311 (bug), 10,951 (release), 7,586 (define), n=4–7 per row. `out=` is undifferentiated from thinking tokens, and `turns` deltas are 0 for all but one pair on these cloud-dispatched runs, so `out/turns` does not work as a denominator here. **After-table blocked:** only berceo has synced (`#26`, from icm-board `f424b49` — stub 1 only, predates #74's loosening), and it has 0 archived runs since — stub stays open, waiting for ≥5 archived runs on a repo synced past #74. |
| 2026-09-24 | — | **The estate on one template — the D40 pass, sustentus on Jamie's word, the intake swept** (`one-branch-two-targets/estate-sync-d39` closed; Jamie's ask: "verify the template work is done, then make every pipeline repo truly up to date"). The three D39 sync PRs had merged at `0db8a71`; D40 stubs 1–2 had moved the template since, so remi-ai, vinecliff and jamienisbet take it again at `ef76473` (k0d0minio/remi-ai#127, k0d0minio/vinecliff#21, k0d0minio/jamienisbet#154 — 13 T files each, the four canonical skills refreshed), and sustentus takes D39 and D40 in one PR, sustentus/sustentus#1158 (32 T files; `promote-uat.sh` + `uat/CONTEXT.md` removed; `uat: {target, url}`; `type:promote` gone from `labels.yml`; its own `mongodb-cleanup.yaml` given the reference's `uat_branch` removal; the canonical five refreshed — all had been byte-identical to their previous canonical, so nothing of sustentus's own was overwritten). T files and skills checked byte-identical on every branch, CI green on all four, every checkout back on `main`; the operator merges. berceo (`f424b49`, pre-#74) and agorasim (`de444cd`) are the cutover session's and were left alone. Every stamp in the estate now names a commit on icm-board's `main` (the `5bef762`/`be76910` stamps are gone) — `triage/icm-sync-stamps-unmerged-commit` stays open as the guard. **Template work:** done but for two phrases in `intake/CONTEXT.md`, fixed in k0d0minio/icm-board#75 (`triage/intake-contract-ticket-pr-wording` closed there) — once it merges the four repos are one T file behind until the next pass. **Intake swept:** three epics with every stub done archived whole (`opencode-executor`, `sustentus-parity`, `vercel-env-system`); `triage/sustentus-web-build-exceeds-vercel-timeout` dropped (did not recur — eight `web` builds READY since, four of them production). Left open on purpose: `terse-sessions/measure-output` (blocked on ≥5 post-sync runs), `triage/icm-sync-stamps-unmerged-commit` (P1), `triage/db-branch-prove-declared-irreversible` (Jamie's decision), `one-branch-two-targets/cutover-agorasim-berceo` (in flight). | — | — |
| 2026-09-24 | — | **berceo cut over to one branch; the UAT database becomes a second Marketplace database — D39 stub 3 (berceo half), D41** (`one-branch-two-targets/cutover-agorasim-berceo`). Steps 0–8 with Jamie: Auto-assign off, custom environment `uat` (branch tracking `main` accepted), k0d0minio/berceo#26 (sync to f424b49; `db-migrate.yml` to the reference gate shape, `release.yaml` new, `report.sh --tag`), the baseline `release/2026-09-24-promote-5916543` published at the sync merge — the first live run of `release.yaml`: stage → migrate → promote (201) → deploy-status, proving the promote call, `targets.production.id`, `customEnvironment.slug` and the migrate gate; k0d0minio/berceo#27 (`main takes uat`, the batch of 3); `uat` deleted from rulesets and remote. The step-8 database read found every UAT build on **production** (D41's account); three Storage configurations tested live with Jamie, the third proven (UAT build and a probe PR's preview in `uat-berceo`, production untouched). Stub amended (the checklist's step 3 is D41's, baseline at the sync merge, the database and preview proofs in step 8); new stub `uat-database-resource` (5 of 5) for the template's two Neon projects. Left: berceo step 9 (founders' sign-off; `uat.berceo.be` CNAME owned by the Berceo team), agorasim (a separate session), the rollout note's agorasim section. |
| 2026-09-24 | — | **The template learns two Neon projects — D41, stub 5 (template half)** (`one-branch-two-targets/uat-database-resource`). `database.neon`: `project_id` stays production's; new `nonprod_project_id` (with UAT: the second Marketplace database's project — UAT is its default branch, previews and runs live in it; without UAT ignored, one project as before) and `reset_command` (the repo's own empty-migrate-seed); `uat_branch` retired — `setup.sh` fails it on any repo. `lib/project.sh`: `neon_nonprod_project_id` (with UAT and no id → `''`, never production's), `neon_split`, `neon_reset_command`; `neon_uat_branch` gone. `lib/neon.sh`: works on the non-production project, `neon_use_project` for reads of production's, one write guard (`create`/`delete` refuse production's project on a UAT repo), `neon_reset_branch` removed (nothing resets from a parent any more). `db-branch.sh`: `run/<slug>` a child of the UAT database there. `db-env.sh`: `status` reads both projects (a `preview/*`/`run/*` left in production's is a `[WARN]`), `init` lists D41's Storage acts, `reset-uat --apply` runs `reset_command` with the url variable on the UAT database's unpooled string (output redacted), a `nonprod_project_id` equal to production's refuses every verb. `setup.sh`: fails `uat_branch`, a UAT repo without `nonprod_project_id` or with production's; with a Vercel token, fails a production `url_env` entry that also targets Preview or a custom environment (`[TODO]` when the API shows none). Reference `neon-cleanup.yaml`: the non-production project on a Neon UAT repo, failing rather than guessing. `env-check.sh`, `promote.sh init` one line each. Words: `_shared/promotion.md` (The UAT database, MongoDB out of D41), `_shared/ci.md`, `_shared/project-rules.md`, `PIPELINE.md`, the `setup` and `database-migration` skills. **Proven on scratch fixtures** against a local mock of the Neon and Vercel APIs (seven repos, 84 cases, no live write): on a D41 repo `neon-cleanup` and `prune --apply` delete only in the non-production project, `db-branch.sh up` creates `run/new-run` there under the UAT database, `reset-uat --apply` hands the command the UAT database's unpooled string — **no write in production's project across every UAT fixture**; `lib/neon.sh` refuses a create and a delete aimed at production's project directly; `setup.sh` fails `uat_branch`, a missing and a production-equal `nonprod_project_id`, and both bad Vercel scopes. **No-UAT fixture: every output byte-identical to before but `reset-uat`'s SKIP line**, which named the retired key. Unproven: the live Vercel env field names; the key reaching both projects. Left open on the stub: berceo and agorasim sync PRs (their `project.json` by hand; `neon-cleanup.yaml` is seeded once, so copied by hand). |
| 2026-09-24 | — | **`db-branch.sh prove` reads a declared-irreversible migration — D35's last follow-up, decision D42** (`triage/db-branch-prove-declared-irreversible` closed). Jamie chose the in-file marker over a `project.json` list or a throw-message convention (D42's row has the reasoning). Template: `db-branch.sh` `prove` — `is_irreversible()` greps each own migration for `irreversible = true`; `boundary_idx` is the newest own migration it matches (or -1); the static "every own file exports a `down`" check skips a declared-irreversible file; the round trip snapshots the shape after the boundary migration's `up` (`s_boundary`, equal to the base shape when there is no boundary) and `down_own()` only reverts own migrations newer than it, per-file `--single` when a boundary or an out-of-order base interleaving is present, the original bulk `down <first>` otherwise (no behaviour change with no marker); the down/up comparison reads back against `s_boundary` instead of always the base shape; the output names the declared-irreversible migration(s) and where the round trip's down stops. The up-then-idempotency check (forget the runner's records, re-apply) is unchanged and still covers every own migration, irreversible or not. Words: the `prove` header block, `PIPELINE.md`, the `database-migration` skill. **Proven on a fixture** (a file-backed fake `mongodb` driver plus a `Migrator.run`-shaped stand-in runner, ts-migrate-mongoose's createdAt semantics, `--base main` against two committed base migrations): a declared-irreversible migration in the middle of three own migrations is skipped and named, only the newer one down-tested, `PROVEN`; the same throwing `down` left undeclared still gives `UNPROVEN 1` (regression held); all three own migrations declared irreversible gives `PROVEN` with the down step never exercised (forward-only-shaped); the baseline three-migration round trip with no marker is `PROVEN` unchanged; a migration with no `down` and no marker still fails the static check, `UNPROVEN 1`. Unproven: a real cluster and the real ts-migrate-mongoose binary (no mongod on this machine, as before); interleaving (out-of-order vs main) combined with a declared-irreversible migration in the same proof — reasoned through, not fixture-run. |
